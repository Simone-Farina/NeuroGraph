# Phase 03-01 Plan: Rigorous Retention Engine

## Objective
Implement a spaced repetition memory engine (using `ts-fsrs`) and its corresponding visual feedback loops.

## Proposed Changes

### Spaced Repetition Core
- Integrate `ts-fsrs` for memory state management.
- Update `Neuron` schema and API to store FSRS parameters.

### Upward FIRe Healing
- Implement recursive prerequisite traversal to apply "shadow reviews" to ancestors when a descendant is mastered.

### Visual Decay (Soft-FIRe)
- Propagate decay status from rusting foundations to dependent neurons with subtle visual warnings.

### Gardener Review Flow
- Surface due reviews organically on the knowledge graph using distinct "rust" coloring.
- Implement `ReviewMode` with Socratic AI escape hatch.

## Verification Plan
- Verify FSRS state updates after a review session.
- Confirm "shadow reviews" propagate correctly to ancestors.
- Audit visual decay propagation in the UI.

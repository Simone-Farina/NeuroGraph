---
phase: 07-queue-triage-ui
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/queue/QueueBootstrap.tsx
  - src/components/queue/__tests__/QueueBootstrap.test.tsx
  - src/components/layout/AppSidebar.tsx
  - src/components/layout/__tests__/AppSidebar.queue.test.tsx
  - src/app/(app)/layout.tsx
autonomous: false
requirements: [TRIAGE-02, TRIAGE-04]

must_haves:
  truths:
    - "The sidebar exposes a Queue route at `/app/queue` with an unread badge that counts Inbox items only."
    - "The badge can return to zero; Passive Debt and Resources never create a permanent backlog alarm."
    - "Queue data stays synchronized across the app shell so captures and queue mutations are reflected in the sidebar without inventing a new left-panel mode."
    - "Returning to the app after an external capture refreshes queue state so new Inbox items surface naturally."
    - "A human verifies the final emotional contract: calm sanctuary, monochrome baseline, semantic rust only for passive debt decay."
  artifacts:
    - path: "src/components/queue/QueueBootstrap.tsx"
      provides: "App-shell queue hydration and refresh-on-return behavior"
      exports: ["QueueBootstrap"]
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "Queue navigation item and Inbox-only badge"
      contains: "Queue"
    - path: "src/app/(app)/layout.tsx"
      provides: "Authenticated shell mount point for queue bootstrap"
      contains: "QueueBootstrap"
    - path: "src/components/layout/__tests__/AppSidebar.queue.test.tsx"
      provides: "Sidebar tests for nav and badge semantics"
      min_lines: 100
    - path: "src/components/queue/__tests__/QueueBootstrap.test.tsx"
      provides: "Bootstrap tests for initial refresh and refetch-on-focus behavior"
      min_lines: 80
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/queue/QueueBootstrap.tsx"
      via: "shell-level queue hydration"
      pattern: "QueueBootstrap"
    - from: "src/components/queue/QueueBootstrap.tsx"
      to: "src/stores/queueStore.ts"
      via: "refreshQueue on mount, focus, and visibility return"
      pattern: "refreshQueue\\("
    - from: "src/components/layout/AppSidebar.tsx"
      to: "src/stores/queueStore.ts"
      via: "Inbox-only badge from shared queue state"
      pattern: "inboxCount"
    - from: "src/components/layout/AppSidebar.tsx"
      to: "/app/queue"
      via: "route-based navigation item"
      pattern: "href: '/app/queue'"
---

<objective>
Integrate the queue module into the authenticated app shell: bootstrap shared queue state, add the Queue navigation item and Inbox-only badge to the sidebar, and finish Phase 7 with a single human verification pass against the emotional and behavioral contract.

Purpose: The queue page is not complete until it lives naturally inside the shell. This plan makes the queue discoverable from navigation, keeps sidebar/page state synchronized, and verifies that the unread badge feels like Inbox Zero relief rather than permanent backlog pressure.

Output: Queue bootstrap component, sidebar integration, and final UAT checkpoint.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-queue-triage-ui/07-CONTEXT.md
@.planning/phases/07-queue-triage-ui/07-RESEARCH.md
@.impeccable.md
@.planning/phases/07-queue-triage-ui/07-01-SUMMARY.md
@.planning/phases/07-queue-triage-ui/07-02-SUMMARY.md
@src/app/(app)/layout.tsx
@src/components/layout/AppSidebar.tsx
@src/stores/queueStore.ts

<interfaces>
From src/stores/queueStore.ts after Plan 07-01:
```typescript
type QueueStore = {
  items: KnowledgeQueueItem[];
  isLoading: boolean;
  error: string | null;
  lastLoadedAt: string | null;
  refreshQueue: () => Promise<void>;
  inboxCount: number;
  pendingCrystallizeItemId: string | null;
};
```

From src/app/(app)/layout.tsx:
```tsx
function AppLayoutContent({ children }: { children: React.ReactNode }) {
  const { user, loading } = useAuth();
  const leftPanelMode = useGraphStore((state) => state.leftPanelMode);

  return (
    <div className="flex h-screen w-full overflow-hidden bg-neural-dark">
      <div className="flex w-[40vw] flex-shrink-0 border-r border-white/5 relative bg-neural-dark">
        <AppSidebar />
        <div className="flex-1 flex flex-col min-w-0 relative">
          <main className="flex-1 overflow-y-auto relative p-0 m-0 w-full h-full">
            {children}
          </main>
        </div>
      </div>
    </div>
  );
}
```

Locked constraints to keep visible while implementing:
```text
- Queue remains a route at /app/queue, never a new leftPanelMode.
- Sidebar badge counts inbox only.
- Passive Debt pressure stays subtle and quiet.
- No loud colored nav badges or anxiety-inducing backlog language.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write shell-integration tests for queue bootstrap and sidebar badge semantics</name>
  <files>src/components/queue/__tests__/QueueBootstrap.test.tsx, src/components/layout/__tests__/AppSidebar.queue.test.tsx</files>
  <action>
Create failing UI contracts before editing the shell.

For `QueueBootstrap.test.tsx` cover:
- initial `refreshQueue()` call when an authenticated user is present,
- no refresh when auth is still loading or there is no user,
- re-fetch when the window regains focus or the document becomes visible again.

For `AppSidebar.queue.test.tsx` cover:
- a Queue nav item linking to `/app/queue`,
- active-state handling for `/app/queue`,
- badge visibility only when `inboxCount > 0`,
- badge value derived from Inbox only,
- collapsed sidebar still preserves discoverability without inventing a new panel mode.

Do not change production components in this task.
  </action>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/queue/__tests__/QueueBootstrap.test.tsx src/components/layout/__tests__/AppSidebar.queue.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Shell-integration tests exist and fail for the missing Queue bootstrap/sidebar behavior.
    - The tests lock Inbox-only badge semantics and route-based navigation expectations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount queue bootstrap in the shell and add the Queue nav item with Inbox-only badge</name>
  <files>src/components/queue/QueueBootstrap.tsx, src/components/queue/__tests__/QueueBootstrap.test.tsx, src/components/layout/AppSidebar.tsx, src/components/layout/__tests__/AppSidebar.queue.test.tsx, src/app/(app)/layout.tsx</files>
  <behavior>
    - The authenticated shell hydrates queue state once and re-syncs when the user returns focus to the app.
    - The sidebar renders a Queue nav link to `/app/queue`.
    - The sidebar badge reflects Inbox count only and disappears cleanly at zero.
    - Queue integration stays route-based and does not expand `graphStore.leftPanelMode`.
  </behavior>
  <action>
Create `src/components/queue/QueueBootstrap.tsx` and mount it inside `src/app/(app)/layout.tsx` near the existing shell composition so queue state is shared across `/app`, `/app/queue`, and `/app/review`.

Implementation requirements:
- `QueueBootstrap` should call `refreshQueue()` when an authenticated user becomes available.
- Add lightweight re-sync on `window.focus` and `document.visibilitychange` so captures created outside the app can appear when the user returns.
- Keep the bootstrap invisible; it is a behavior-only shell component.

Then update `AppSidebar.tsx`:
- add a Queue nav item pointing to `/app/queue`,
- position it with the existing primary nav, between Chat and Review,
- derive badge count from `useQueueStore((state) => state.inboxCount)`,
- make the badge monochrome and small; it should read like an unread indicator, not an alert,
- hide the badge entirely at zero,
- preserve collapsed-sidebar behavior without introducing a new queue panel mode.

Do not add dashboard copy, red warning badges, or any new global notification system here.
  </action>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/queue/__tests__/QueueBootstrap.test.tsx src/components/layout/__tests__/AppSidebar.queue.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Queue state hydrates at the shell level.
    - The sidebar includes a Queue route with an Inbox-only unread badge.
    - Badge state stays synchronized with queue mutations and newly returned captures.
    - The app still uses route navigation rather than `leftPanelMode` for queue access.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the final Queue shell integration and emotional contract</name>
  <files>src/components/layout/AppSidebar.tsx, src/components/queue/QueueBootstrap.tsx, src/app/(app)/layout.tsx</files>
  <action>
Pause after Task 2 passes its automated checks and request a single human verification pass covering the full Queue flow, sidebar synchronization, and the low-anxiety design brief.
  </action>
  <what-built>Complete Phase 7 Queue Triage UI inside the authenticated shell, including queue page, inline triage actions, Inbox-only sidebar badge, and quiet Passive Debt aging/rust semantics.</what-built>
  <how-to-verify>
Visit `/app/queue` in a signed-in session and verify:
1. The page reads as one calm editorial column inside the existing 40vw left panel, with sections ordered exactly: Inbox, Passive Debt, Resources.
2. The sidebar has a Queue nav item and the badge equals the number of Inbox items only.
3. Opening an Inbox item URL opens the external destination normally and the item moves to Passive Debt; the sidebar badge decrements immediately.
4. `Mark as Read`, `Archive`, and `Delete` update state without a hard refresh, and the sidebar badge never counts Passive Debt or Resources.
5. Clicking `Crystallize` returns you to `/app` chat view with no seeded-summary experience yet; that remains deferred to Phase 8.
6. An aged Passive Debt item shows a human-readable age label and muted terracotta/rust semantics rather than bright error red.
  </how-to-verify>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/queue/__tests__/QueueBootstrap.test.tsx src/components/layout/__tests__/AppSidebar.queue.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - The user confirms the Queue Triage UI matches the behavioral and visual brief, or supplies concrete mismatches for follow-up.
  </done>
  <resume-signal>Type `approved` if the Queue Triage UI matches the brief, or describe any mismatches that need correction.</resume-signal>
</task>

</tasks>

<verification>
Run the shell integration tests and type-check the project before the human checkpoint:

```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/queue/__tests__/QueueBootstrap.test.tsx src/components/layout/__tests__/AppSidebar.queue.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<success_criteria>
- Queue is discoverable from the sidebar and feels native to the shell.
- The unread badge behaves like Inbox Zero relief, not permanent backlog stress.
- Queue state refreshes when the user returns to the app after external capture activity.
- A single final human verification confirms both the behavior and the emotional contract.
</success_criteria>

<output>
After completion, create `.planning/phases/07-queue-triage-ui/07-03-SUMMARY.md`
</output>

---
phase: 08-crystallize-flow
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/chat/CrystallizeBootstrap.tsx
  - src/components/chat/CrystallizePasteComposer.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/__tests__/ChatPanel.crystallize.test.tsx
autonomous: true
requirements: [CRYST-01, CRYST-02]

must_haves:
  truths:
    - "Entering crystallize from the queue opens `/app` and begins a calm in-chat bootstrap instead of a modal or route wizard."
    - "A seeded conversation appears automatically when backend crystallize succeeds."
    - "When backend crystallize returns `awaiting_manual_paste`, the chat surface renders an embedded paste composer inside the same conversation."
    - "The chat transcript stays editorial; loading/fallback UI does not feel like an error dashboard."
    - "Queue-side crystallize intent is cleared only after the chat surface has a conversation to continue."
  artifacts:
    - path: "src/components/chat/CrystallizeBootstrap.tsx"
      provides: "Chat-surface crystallize starter that consumes queue intent and calls `/api/crystallize`"
      exports: ["CrystallizeBootstrap"]
    - path: "src/components/chat/CrystallizePasteComposer.tsx"
      provides: "Embedded manual-paste continuation surface for `awaiting_manual_paste` conversations"
      exports: ["CrystallizePasteComposer"]
    - path: "src/components/chat/ChatPanel.tsx"
      provides: "Integrated crystallize bootstrap, manual-paste state derivation, and calm in-chat loading/error handling"
      exports: ["ChatPanel"]
    - path: "src/components/chat/__tests__/ChatPanel.crystallize.test.tsx"
      provides: "Component tests for seeded handoff and manual-paste continuation"
      min_lines: 140
  key_links:
    - from: "src/components/chat/CrystallizeBootstrap.tsx"
      to: "src/stores/queueStore.ts"
      via: "consume `pendingCrystallizeItemId` and clear intent after successful handoff"
      pattern: "pendingCrystallizeItemId"
    - from: "src/components/chat/CrystallizeBootstrap.tsx"
      to: "/api/crystallize"
      via: "start crystallize conversation"
      pattern: "fetch\\('/api/crystallize'"
    - from: "src/components/chat/CrystallizePasteComposer.tsx"
      to: "/api/crystallize/manual"
      via: "manual paste continuation"
      pattern: "fetch\\('/api/crystallize/manual'"
    - from: "src/components/chat/ChatPanel.tsx"
      to: "src/lib/contexts/ConversationContext.tsx"
      via: "set the created conversation id and refresh sidebar conversations"
      pattern: "setCurrentConversationId"
---

<objective>
Integrate the Phase 8 backend flow into the existing `/app` chat surface: detect queue-side crystallize intent, bootstrap a seeded conversation, and render the manual-paste fallback inline when extraction fails.

Purpose: Users should feel like crystallize simply deepens the current product rhythm. The chat panel must absorb the new backend route cleanly and preserve the low-anxiety Danish Computation contract.

Output: crystallize bootstrap component, embedded paste composer, `ChatPanel` integration, and component tests.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-crystallize-flow/08-CONTEXT.md
@.planning/phases/08-crystallize-flow/08-RESEARCH.md
@.planning/phases/08-crystallize-flow/08-VALIDATION.md
@.impeccable.md
@.planning/phases/07-queue-triage-ui/07-03-SUMMARY.md
@src/stores/queueStore.ts
@src/components/chat/ChatPanel.tsx
@src/lib/contexts/ConversationContext.tsx
@src/app/api/chat/route.ts

<interfaces>
Frontend behavior to implement in this plan:
```typescript
type StartCrystallizeResponse =
  | { conversationId: string; queueItemId: string; mode: 'seeded' }
  | {
      conversationId: string;
      queueItemId: string;
      mode: 'awaiting_manual_paste';
      reason: 'missing_url' | 'timeout' | 'paywall' | 'empty' | 'unsupported';
    };
```

Chat UI state to derive from loaded messages:
```typescript
type ActiveCrystallizeSession =
  | null
  | {
      queueItemId: string;
      status: 'seeded' | 'awaiting_manual_paste';
      failureReason?: 'missing_url' | 'timeout' | 'paywall' | 'empty' | 'unsupported';
    };
```

Visible fallback copy should stay restrained:
```text
Paste the source text and continue.
We will compress it into a briefing and continue the conversation.
```
</interfaces>
</context>

<tasks>

<task id="08-02-01" type="auto" tdd="true">
  <name>Task 1: Bootstrap crystallize intent into a real conversation inside ChatPanel</name>
  <files>src/components/chat/CrystallizeBootstrap.tsx, src/components/chat/ChatPanel.tsx, src/components/chat/__tests__/ChatPanel.crystallize.test.tsx</files>
  <read_first>
    - src/components/chat/ChatPanel.tsx
    - src/stores/queueStore.ts
    - src/lib/contexts/ConversationContext.tsx
    - src/app/api/crystallize/route.ts
    - .impeccable.md
    - .planning/phases/08-crystallize-flow/08-RESEARCH.md
  </read_first>
  <action>
Create `src/components/chat/CrystallizeBootstrap.tsx` as a behavior-only child mounted inside `ChatPanel`.

Implementation requirements:
- Read `pendingCrystallizeItemId` from `useQueueStore`.
- When `pendingCrystallizeItemId` exists and there is no active bootstrap request, call `POST /api/crystallize` with `{ queueItemId }`.
- While the request is in flight, surface a quiet loading state inside the chat column using subdued copy such as `Preparing source…`; do not open a modal, toast storm, or full-screen blocker.
- On success, call `setCurrentConversationId(response.conversationId)`, then `refreshConversations()`, then `clearCrystallizeIntent()`.
- If the route returns `mode: 'awaiting_manual_paste'`, still set the returned conversation id and clear the queue intent so the conversation becomes the new source of truth.
- On hard failure before a conversation id exists, clear the queue intent and surface a restrained inline error string in the chat area; do not redirect back to `/app/queue`.

Update `ChatPanel.tsx` to mount `CrystallizeBootstrap`, track `isCrystallizing`, and avoid conflicting with the normal first-user-message flow while crystallize bootstrap is active.

Write `src/components/chat/__tests__/ChatPanel.crystallize.test.tsx` covering:
- queue intent triggers `/api/crystallize`,
- successful response sets conversation id and clears queue intent,
- `awaiting_manual_paste` response also sets conversation id,
- hard failure surfaces inline feedback without modal/theater.
  </action>
  <acceptance_criteria>
    - src/components/chat/CrystallizeBootstrap.tsx exists and reads `pendingCrystallizeItemId`
    - src/components/chat/CrystallizeBootstrap.tsx calls `fetch('/api/crystallize'`
    - src/components/chat/ChatPanel.tsx renders `CrystallizeBootstrap`
    - src/components/chat/ChatPanel.tsx contains a crystallize loading branch with `Preparing source`
    - src/components/chat/__tests__/ChatPanel.crystallize.test.tsx asserts conversation id assignment and queue intent clearing
    - `npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Queue-side crystallize intent becomes a real chat conversation.
    - Chat bootstrap remains calm and route-based.
    - The existing chat flow is not broken for normal conversations.
  </done>
</task>

<task id="08-02-02" type="auto" tdd="true">
  <name>Task 2: Render embedded manual-paste continuation for `awaiting_manual_paste` conversations</name>
  <files>src/components/chat/CrystallizePasteComposer.tsx, src/components/chat/ChatPanel.tsx, src/components/chat/__tests__/ChatPanel.crystallize.test.tsx</files>
  <read_first>
    - src/components/chat/ChatPanel.tsx
    - src/components/chat/MessageList.tsx
    - src/app/api/chat/route.ts
    - src/app/api/crystallize/manual/route.ts
    - .impeccable.md
    - .planning/phases/08-crystallize-flow/08-CONTEXT.md
  </read_first>
  <action>
Create `src/components/chat/CrystallizePasteComposer.tsx` and render it from `ChatPanel.tsx` when the loaded conversation messages contain crystallize metadata with `status: 'awaiting_manual_paste'`.

Implementation requirements:
- Derive active crystallize session state by inspecting loaded message metadata already returned by `/api/chat?mode=messages`.
- Show the composer above the normal chat input, inside the chat column, using calm monochrome styling and the exact helper copy:
  - `Paste the source text and continue.`
  - `We will compress it into a briefing and continue the conversation.`
- Use a multiline textarea and a subdued submit button label `Continue`.
- Submit to `POST /api/crystallize/manual` with `{ conversationId, queueItemId, pastedText }`.
- While submission is in flight, disable the composer and show a restrained pending label such as `Compressing source…`.
- On success, call the existing conversation reload path so the new seeded assistant message appears and the composer disappears.
- Keep the raw pasted text out of the visible message transcript; only the backend marker message should appear in history.

Extend `src/components/chat/__tests__/ChatPanel.crystallize.test.tsx` to cover:
- fallback metadata causes the composer to render,
- successful manual paste submission calls `/api/crystallize/manual` and reloads the conversation,
- the composer disappears once the conversation becomes `seeded`.
  </action>
  <acceptance_criteria>
    - src/components/chat/CrystallizePasteComposer.tsx exists
    - src/components/chat/ChatPanel.tsx derives crystallize session state from message metadata
    - src/components/chat/ChatPanel.tsx renders the exact copy `Paste the source text and continue.`
    - src/components/chat/CrystallizePasteComposer.tsx calls `fetch('/api/crystallize/manual'`
    - src/components/chat/__tests__/ChatPanel.crystallize.test.tsx covers fallback render and successful continuation
    - `npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Failed extraction no longer dead-ends the user.
    - Manual paste remains inside the same conversation and visual rhythm.
    - Chat transcript stays editorial instead of becoming a raw ingest dump.
  </done>
</task>

</tasks>

<verification>
Run the crystallize chat-surface tests and type-check:

```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<success_criteria>
- Crystallize can begin from the queue and materialize as a chat conversation without user typing.
- Manual paste fallback lives inside the same chat flow and matches the low-anxiety design contract.
- The chat surface stays editorial, quiet, and route-based.
</success_criteria>

<output>
After completion, create `.planning/phases/08-crystallize-flow/08-02-SUMMARY.md`
</output>

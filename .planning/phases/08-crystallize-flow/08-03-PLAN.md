---
phase: 08-crystallize-flow
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/lib/crystallize/provenance.ts
  - src/lib/crystallize/__tests__/provenance.test.ts
  - src/app/api/neurons/route.ts
  - src/app/api/neurons/__tests__/route.test.ts
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/__tests__/ChatPanel.mastery.test.tsx
autonomous: false
requirements: [CRYST-03]

must_haves:
  truths:
    - "Neuron creation can detect whether its `source_conversation_id` came from a crystallize session."
    - "Queue mastery respects the existing state machine by walking allowed transitions instead of bypassing queue rules."
    - "Repeated Neurogenesis from the same crystallize conversation is idempotent for queue mastery."
    - "The client refreshes queue state after successful mastery so the queue reflects the new reality without waiting for app-focus refresh."
    - "A final human pass confirms successful seed flow, fallback continuity, and mastered-state reflection."
  artifacts:
    - path: "src/lib/crystallize/provenance.ts"
      provides: "Conversation-to-queue provenance resolution and mastered-transition helper"
      exports: ["resolveCrystallizeQueueItemId", "advanceQueueItemToMastered"]
    - path: "src/app/api/neurons/route.ts"
      provides: "Neurogenesis route with crystallize mastery handoff"
      exports: ["POST"]
    - path: "src/components/chat/__tests__/ChatPanel.mastery.test.tsx"
      provides: "Client integration tests for queue refresh after mastery"
      min_lines: 100
  key_links:
    - from: "src/lib/crystallize/provenance.ts"
      to: "src/app/api/chat/route.ts"
      via: "resolve provenance from `messages.metadata` loaded by conversation id"
      pattern: "metadata"
    - from: "src/lib/crystallize/provenance.ts"
      to: "src/lib/db/queueQueries.ts"
      via: "walk queue state transitions through `updateState`"
      pattern: "queueQueries\\.updateState\\("
    - from: "src/app/api/neurons/route.ts"
      to: "src/lib/crystallize/provenance.ts"
      via: "resolve and advance crystallize-linked queue item"
      pattern: "(resolveCrystallizeQueueItemId|advanceQueueItemToMastered)"
    - from: "src/components/chat/ChatPanel.tsx"
      to: "src/stores/queueStore.ts"
      via: "refresh queue after a mastered handoff"
      pattern: "refreshQueue\\("
---

<objective>
Close the Phase 8 loop by connecting crystallize provenance to Neurogenesis: detect the originating queue item from conversation metadata, advance it to `mastered` through the existing queue state machine, refresh queue state in the client, and finish with a human verification checkpoint.

Purpose: Phase 8 is not done when a seeded chat exists; it is done when a successful learning session can complete the queue lifecycle without ambiguity or manual cleanup.

Output: provenance helpers, neuron-route mastery handoff, queue refresh integration, and final UAT checkpoint.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-crystallize-flow/08-CONTEXT.md
@.planning/phases/08-crystallize-flow/08-RESEARCH.md
@.planning/phases/08-crystallize-flow/08-VALIDATION.md
@.planning/phases/08-crystallize-flow/08-01-PLAN.md
@.planning/phases/08-crystallize-flow/08-02-PLAN.md
@src/app/api/neurons/route.ts
@src/lib/db/queueQueries.ts
@src/components/chat/ChatPanel.tsx
@src/stores/queueStore.ts

<interfaces>
Server helper behavior to create in this plan:
```typescript
async function resolveCrystallizeQueueItemId(
  supabase: SupabaseClient<Database>,
  conversationId: string
): Promise<string | null>;

async function advanceQueueItemToMastered(
  supabase: SupabaseClient<Database>,
  queueItemId: string
): Promise<'mastered' | 'already_mastered' | 'not_found'>;
```

`POST /api/neurons` response should extend the success shape with:
```typescript
{
  neuron: ...,
  synapses?: ...,
  synapse_suggestions?: ...,
  mastered_queue_item_id?: string
}
```
</interfaces>
</context>

<tasks>

<task id="08-03-01" type="auto" tdd="true">
  <name>Task 1: Resolve crystallize provenance and master queue items through allowed transitions</name>
  <files>src/lib/crystallize/provenance.ts, src/lib/crystallize/__tests__/provenance.test.ts, src/app/api/neurons/route.ts, src/app/api/neurons/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/neurons/route.ts
    - src/lib/db/queueQueries.ts
    - src/lib/crystallize/types.ts
    - src/app/api/chat/route.ts
    - src/types/database.ts
    - .planning/phases/08-crystallize-flow/08-RESEARCH.md
  </read_first>
  <action>
Create `src/lib/crystallize/provenance.ts` with:
- `resolveCrystallizeQueueItemId(...)` that selects `metadata` from `messages` for the supplied `conversationId`, scans in ascending `created_at` order, and returns the first `metadata.crystallize.queue_item_id` string it finds.
- `advanceQueueItemToMastered(...)` that loads the queue item and walks these exact allowed paths using `queueQueries.updateState` only:
  - `inbox -> passive_debt -> mastered`
  - `resource -> passive_debt -> mastered`
  - `passive_debt -> mastered`
  - `mastered` -> no-op result `already_mastered`

Update `src/app/api/neurons/route.ts` after neuron creation succeeds:
- resolve the crystallize queue item id from `source_conversation_id`,
- if found, call `advanceQueueItemToMastered(...)`,
- include `mastered_queue_item_id` in the JSON response when the result is `mastered` or `already_mastered`,
- leave non-crystallize conversations unchanged.

Write tests:
- `src/lib/crystallize/__tests__/provenance.test.ts` for provenance lookup and transition walking
- extend `src/app/api/neurons/__tests__/route.test.ts` to cover crystallize-linked mastery, `resource -> passive_debt -> mastered`, and idempotent repeated Neurogenesis

Do not bypass queue rules with a raw `UPDATE state = 'mastered'`.
  </action>
  <acceptance_criteria>
    - src/lib/crystallize/provenance.ts exports `resolveCrystallizeQueueItemId`
    - src/lib/crystallize/provenance.ts exports `advanceQueueItemToMastered`
    - src/lib/crystallize/provenance.ts contains the exact transition path `resource -> passive_debt -> mastered`
    - src/app/api/neurons/route.ts includes `mastered_queue_item_id`
    - src/app/api/neurons/__tests__/route.test.ts covers crystallize-linked mastery and repeated Neurogenesis idempotence
    - `npx vitest run src/lib/crystallize/__tests__/provenance.test.ts src/app/api/neurons/__tests__/route.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/lib/crystallize/__tests__/provenance.test.ts src/app/api/neurons/__tests__/route.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Neurogenesis can close the loop on crystallize-linked queue items.
    - Mastered transition stays compatible with the existing state machine.
    - Repeated neuron creation does not create ambiguous queue outcomes.
  </done>
</task>

<task id="08-03-02" type="auto" tdd="true">
  <name>Task 2: Refresh queue state in the client after mastered handoff</name>
  <files>src/components/chat/ChatPanel.tsx, src/components/chat/__tests__/ChatPanel.mastery.test.tsx</files>
  <read_first>
    - src/components/chat/ChatPanel.tsx
    - src/stores/queueStore.ts
    - src/app/api/neurons/route.ts
    - src/components/chat/__tests__/ChatPanel.crystallize.test.tsx
    - .planning/phases/08-crystallize-flow/08-CONTEXT.md
  </read_first>
  <action>
Update `ChatPanel.tsx` so that after successful Neurogenesis:
- if the `/api/neurons` response contains `mastered_queue_item_id`, call `useQueueStore.getState().refreshQueue()`,
- clear `pendingCrystallizeItemId` if it still matches the mastered item,
- optionally show a restrained confirmation string such as `Queue item mastered` using the existing quiet notice pattern,
- do not change normal non-crystallize Neurogenesis behavior.

Create `src/components/chat/__tests__/ChatPanel.mastery.test.tsx` covering:
- queue refresh fires when `mastered_queue_item_id` exists,
- queue refresh does not fire for normal neuron creation responses without crystallize provenance,
- confirmation notice stays low-anxiety and does not use warning/error styling.
  </action>
  <acceptance_criteria>
    - src/components/chat/ChatPanel.tsx calls `useQueueStore.getState().refreshQueue()`
    - the refresh call is gated on `mastered_queue_item_id`
    - src/components/chat/__tests__/ChatPanel.mastery.test.tsx exists
    - `npx vitest run src/components/chat/__tests__/ChatPanel.mastery.test.tsx --reporter=verbose` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/components/chat/__tests__/ChatPanel.mastery.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Queue state reflects crystallize mastery without waiting for app focus or a full reload.
    - Normal neuron creation remains unaffected.
  </done>
</task>

<task id="08-03-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the full crystallize loop</name>
  <files>src/app/api/crystallize/route.ts, src/components/chat/ChatPanel.tsx, src/app/api/neurons/route.ts</files>
  <action>
Pause after Tasks 1 and 2 pass automated checks and request one human verification pass covering successful seed flow, manual paste fallback, and the mastered handoff.
  </action>
  <what-built>Complete Phase 8 Crystallize Flow from queue item to seeded chat session to mastered queue state after successful Neurogenesis.</what-built>
  <how-to-verify>
In a signed-in session:
1. From `/app/queue`, click `Crystallize` on a URL-backed item that is readable and confirm `/app` opens into a seeded conversation with source framing, concise assistant briefing, and one Socratic opening question.
2. Force an extraction failure (paywalled URL, broken URL, or no-URL item) and confirm the same chat flow renders an embedded manual paste surface with subdued styling rather than bouncing back to queue.
3. Paste source text, continue, and confirm the seeded assistant message appears in the same conversation.
4. Trigger Neurogenesis from that crystallize-linked conversation and confirm the originating queue item leaves active queue sections after the queue refresh.
5. Repeat Neurogenesis from the same conversation and confirm the queue state does not bounce or duplicate mastery side effects.
  </how-to-verify>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx vitest run src/lib/crystallize/__tests__/provenance.test.ts src/app/api/neurons/__tests__/route.test.ts src/components/chat/__tests__/ChatPanel.mastery.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - The user confirms the full crystallize loop works and feels calm, or provides concrete mismatches.
  </done>
  <resume-signal>Type `approved` if the Crystallize Flow matches the brief, or describe the mismatches.</resume-signal>
</task>

</tasks>

<verification>
Run the mastery/provenance tests and type-check:

```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/lib/crystallize/__tests__/provenance.test.ts src/app/api/neurons/__tests__/route.test.ts src/components/chat/__tests__/ChatPanel.mastery.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<success_criteria>
- Creating a neuron from a crystallize-linked conversation advances the correct queue item to `mastered`.
- Queue mastery is idempotent and respects the existing state machine.
- The app shell reflects the mastery quickly enough that the user sees the queue relationship close.
</success_criteria>

<output>
After completion, create `.planning/phases/08-crystallize-flow/08-03-SUMMARY.md`
</output>

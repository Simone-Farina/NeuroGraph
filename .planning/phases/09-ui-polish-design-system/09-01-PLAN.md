---
phase: 09-ui-polish-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/layout.tsx
  - src/components/layout/AppSidebar.tsx
  - src/stores/graphStore.ts
  - src/components/layout/__tests__/AppSidebar.queue.test.tsx
autonomous: true
requirements: [POLISH-02, POLISH-05]

must_haves:
  truths:
    - "The shell no longer hardcodes one 40/60 split for every task state."
    - "Layout behavior is driven by named presets, not drag resizing."
    - "Conversation history is curated into Active/Recent and Fading groups."
    - "Fading conversations use restrained terracotta semantics and lower opacity instead of loud warning styling."
    - "Shell transitions feel intentional and low-anxiety."
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "Animated shell preset layout for left/right panel widths"
      exports: ["AppLayout"]
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "Curated conversation history with Active/Recent and Fading groups"
      exports: ["AppSidebar"]
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/stores/graphStore.ts"
      via: "shell preset state and panel-mode coordination"
      pattern: "(shellPreset|leftPanelMode)"
    - from: "src/components/layout/AppSidebar.tsx"
      to: "src/lib/contexts/ConversationContext"
      via: "conversation grouping based on existing conversation timestamps"
      pattern: "conversations"
---

<objective>
Establish the structural polish foundation for Phase 9: named shell presets, animated panel width changes, and a curated conversation-history model that communicates fading memory instead of clutter.

Purpose: The shell and history are the primary structural problems. Fixing them first sets the tone for the rest of the editorial redesign.

Output: shell preset state, animated layout behavior, grouped sidebar history, and focused tests.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-design-system/09-CONTEXT.md
@.planning/phases/09-ui-polish-design-system/09-RESEARCH.md
@.planning/phases/09-ui-polish-design-system/09-VALIDATION.md
@.planning/phases/07-queue-triage-ui/07-UI-REVIEW.md
@.impeccable.md
@src/app/(app)/layout.tsx
@src/components/layout/AppSidebar.tsx
@src/stores/graphStore.ts

<tasks>

<task id="09-01-01" type="auto" tdd="true">
  <name>Task 1: Introduce named shell presets and animated panel widths</name>
  <files>src/app/(app)/layout.tsx, src/stores/graphStore.ts</files>
  <action>
Add a shell preset model with exactly these preset names:
- `standard`
- `deep_read`
- `graph_zenith`

Drive the left/right panel widths from the preset state instead of hardcoded `w-[40vw]` and `w-[60vw]`.

Implementation rules:
- No drag-to-resize
- Animate preset transitions with Framer Motion
- Keep `standard` as the default preset
- Support route/content-aware preset changes for review and graph-heavy contexts
- Keep shell behavior low-anxiety and deterministic rather than hyper-reactive
  </action>
  <acceptance_criteria>
    - the shell no longer relies on one fixed 40/60 width pair
    - preset state is stored centrally
    - `standard`, `deep_read`, and `graph_zenith` all exist explicitly
    - transitions between presets are animated
  </acceptance_criteria>
</task>

<task id="09-01-02" type="auto" tdd="true">
  <name>Task 2: Rework the sidebar history into Active/Recent and Fading</name>
  <files>src/components/layout/AppSidebar.tsx, src/components/layout/__tests__/AppSidebar.queue.test.tsx</files>
  <action>
Replace the current uniform conversation stack with a curated history model:
- `Active/Recent`: last 24-48h, solid text
- `Fading`: sessions approaching TTL, rust semantics + reduced opacity

Implementation rules:
- Reduce metadata noise and repeated countdown clutter
- Preserve low-anxiety tone
- Titles should be more visually legible than the metadata
- Fading must read as semantic decay, not error state
- Keep the queue badge behavior intact
  </action>
  <acceptance_criteria>
    - conversations are visually grouped into Active/Recent and Fading
    - fading rows use restrained terracotta semantics and lower opacity
    - queue nav and inbox badge still function correctly
    - focused sidebar tests pass
  </acceptance_criteria>
</task>

<task id="09-01-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Human-check the shell structure before deeper surface polish</name>
  <files>src/app/(app)/layout.tsx, src/components/layout/AppSidebar.tsx</files>
  <action>
Pause after Tasks 1 and 2 pass and confirm that the shell now feels structurally calmer before proceeding to editorial chat and empty-state polish.
  </action>
</task>

</tasks>

<verification>
Run focused shell/sidebar checks:

```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/layout/__tests__/AppSidebar.queue.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<success_criteria>
- The left/right shell can enter named presets without drag complexity.
- History feels curated rather than cluttered.
- Fading conversations visually tell the TTL story.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-design-system/09-01-SUMMARY.md`
</output>

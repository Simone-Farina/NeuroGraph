---
phase: 09-ui-polish-design-system
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/chat/MessageList.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/graph/GraphPanel.tsx
  - src/components/queue/QueuePageClient.tsx
  - src/components/queue/QueueItemCard.tsx
  - src/components/queue/QueueSection.tsx
  - src/app/globals.css
autonomous: true
requirements: [POLISH-01, POLISH-03]

must_haves:
  truths:
    - "Assistant messages render as editorial prose rather than bordered chat bubbles."
    - "Empty states in chat and graph teach the interface in brand voice and avoid template-like emoji theater."
    - "The main page no longer feels chat-dominant on first load."
    - "Deferred Phase 7 queue polish is absorbed into the broader editorial left-panel system."
  artifacts:
    - path: "src/components/chat/MessageList.tsx"
      provides: "Editorial assistant-message rendering and calmer chat empty state"
      exports: ["MessageList"]
    - path: "src/components/graph/GraphPanel.tsx"
      provides: "Brand-aligned graph empty state"
      exports: ["GraphPanel"]
    - path: "src/components/queue/QueueItemCard.tsx"
      provides: "Flatter editorial queue row treatment"
      exports: ["QueueItemCard"]
  key_links:
    - from: "src/components/chat/MessageList.tsx"
      to: "src/components/chat/ChatPanel.tsx"
      via: "shared left-panel editorial composition"
      pattern: "MessageList"
    - from: "src/components/queue/QueueItemCard.tsx"
      to: ".planning/phases/07-queue-triage-ui/07-UI-REVIEW.md"
      via: "remaining queue polish carried into Phase 9"
      pattern: "(terracotta|flatter|micro-type)"
---

<objective>
Convert the main app surfaces into a cohesive editorial workspace: quieter empty/new-account states, prose-first assistant rendering, and queue styling that matches the same design language.

Purpose: Once the shell is structurally correct, the inner reading surfaces need to stop looking like generic widgets and start feeling like a professional cognitive lab.

Output: editorial chat presentation, redesigned empty states, stabilized semantic tokens, and queue follow-through from the Phase 7 audit.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-design-system/09-CONTEXT.md
@.planning/phases/09-ui-polish-design-system/09-RESEARCH.md
@.planning/phases/09-ui-polish-design-system/09-VALIDATION.md
@.planning/phases/07-queue-triage-ui/07-UI-REVIEW.md
@.impeccable.md
@src/components/chat/MessageList.tsx
@src/components/chat/ChatPanel.tsx
@src/components/graph/GraphPanel.tsx
@src/components/queue/QueuePageClient.tsx
@src/components/queue/QueueItemCard.tsx
@src/components/queue/QueueSection.tsx
@src/app/globals.css

<tasks>

<task id="09-02-01" type="auto" tdd="true">
  <name>Task 1: Redesign chat rendering into a calm editorial workspace</name>
  <files>src/components/chat/MessageList.tsx, src/components/chat/ChatPanel.tsx, src/app/globals.css</files>
  <action>
Rework chat presentation so assistant output reads like editorial prose rather than bubble chat.

Implementation rules:
- Assistant messages become full-width or near-full-width prose blocks with minimal chrome
- User messages become quieter annotations, not equally weighted bubbles
- The empty/new-account state must feel like a quiet desk with oversized typography and subdued structure
- The bottom input bar becomes more understated and integrated with the workspace
  </action>
  <acceptance_criteria>
    - assistant output is no longer rendered as heavy bordered bubbles
    - the left panel empty state feels calm and purposeful
    - the input bar is visually understated
  </acceptance_criteria>
</task>

<task id="09-02-02" type="auto" tdd="true">
  <name>Task 2: Align graph and queue surfaces with the same editorial system</name>
  <files>src/components/graph/GraphPanel.tsx, src/components/queue/QueuePageClient.tsx, src/components/queue/QueueItemCard.tsx, src/components/queue/QueueSection.tsx, src/app/globals.css</files>
  <action>
Bring the graph empty state and queue surfaces into the same Phase 9 design system.

Implementation rules:
- Remove template-like emoji/celebratory empty-state tropes
- Flatten queue rows further and simplify the micro-type hierarchy
- Stabilize terracotta/rust semantics into reusable styling instead of raw one-off orange utilities
- Preserve the existing low-anxiety queue model
  </action>
  <acceptance_criteria>
    - graph empty state teaches the interface without emoji-in-circle patterns
    - queue rows feel flatter and more editorial
    - terracotta/rust semantics are reusable and restrained
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run focused presentation and type checks:

```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/chat/__tests__/ChatPanel.crystallize.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<success_criteria>
- The main page feels like a professional editorial workspace on first load.
- Chat and queue surfaces now belong to the same visual system.
- Empty states teach without sounding generic.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-design-system/09-02-SUMMARY.md`
</output>

---
phase: 10-promptfoo-evaluation-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prompt-eval/
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "promptfoo is installed as a project-local dev dependency, never as a global tool."
    - "The repository contains a canonical root `prompt-eval/` workspace."
    - "The project exposes one canonical full-suite eval command and per-agent runner commands."
    - "Future agent suites can be added without restructuring the evaluation root."
  artifacts:
    - path: "package.json"
      provides: "Project-local promptfoo dev dependency and canonical eval scripts"
      exports: ["scripts.eval:all", "scripts.eval:bouncer", "scripts.eval:architect", "scripts.eval:conversationalist"]
    - path: "prompt-eval/"
      provides: "Dedicated prompt evaluation root with per-agent subdirectories and shared scaffolding"
      exports: ["bouncer", "architect", "conversationalist", "shared"]
  key_links:
    - from: "package.json"
      to: "prompt-eval/bouncer/promptfooconfig.yaml"
      via: "eval:bouncer script"
      pattern: "eval:bouncer"
    - from: "package.json"
      to: "prompt-eval/architect/promptfooconfig.yaml"
      via: "eval:architect script"
      pattern: "eval:architect"
    - from: "package.json"
      to: "prompt-eval/conversationalist/promptfooconfig.yaml"
      via: "eval:conversationalist script"
      pattern: "eval:conversationalist"
---

<objective>
Stand up the Phase 10 infrastructure: install `promptfoo` locally, create the root `prompt-eval/` workspace, and expose the canonical local runner commands.

Purpose: Establish the harness foundation before writing the first Golden suite.

Output: local dependency, package scripts, root evaluation directories, and shared scaffolding.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-promptfoo-evaluation-harness/10-CONTEXT.md
@.planning/phases/10-promptfoo-evaluation-harness/10-RESEARCH.md
@.planning/phases/10-promptfoo-evaluation-harness/10-VALIDATION.md
@package.json

<tasks>

<task id="10-01-01" type="auto" tdd="true">
  <name>Task 1: Install promptfoo locally and add canonical package scripts</name>
  <files>package.json, package-lock.json</files>
  <action>
Install `promptfoo` strictly as a dev dependency with npm and add exactly these scripts:
- `eval:all`
- `eval:bouncer`
- `eval:architect`
- `eval:conversationalist`

Implementation rules:
- no global install
- no custom global wrapper requirement
- `eval:all` must resolve through the local project dependency
  </action>
  <acceptance_criteria>
    - `package.json` lists `promptfoo` under devDependencies
    - `package.json` exposes the four eval scripts
    - the dependency is installed via local project metadata only
  </acceptance_criteria>
</task>

<task id="10-01-02" type="auto" tdd="true">
  <name>Task 2: Create the canonical prompt-eval root and shared scaffolding</name>
  <files>prompt-eval/</files>
  <action>
Create the dedicated root workspace:
- `prompt-eval/shared/`
- `prompt-eval/bouncer/`
- `prompt-eval/architect/`
- `prompt-eval/conversationalist/`

Include only the minimal shared scaffolding needed to keep suites coherent, such as:
- shared README or conventions
- shared helper/assertion entry points if required
- placeholder configs or stubs for architect and conversationalist

Implementation rules:
- keep runtime prompt code out of this directory
- avoid overengineering abstractions before the first suite exists
  </action>
  <acceptance_criteria>
    - the `prompt-eval/` root exists
    - all three agent directories exist
    - shared scaffolding exists without bloating production code
  </acceptance_criteria>
</task>

<task id="10-01-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Confirm the harness shape before investing in suite details</name>
  <files>package.json, prompt-eval/</files>
  <action>
Pause after the infrastructure is in place and confirm the command names and root structure feel clean enough to scale into phases 11-13.
  </action>
</task>

</tasks>

<verification>
Run the infrastructure smoke checks:

```bash
cd /home/simone/projects/NeuroGraph
npm run eval:all
npx tsc --noEmit
```
</verification>

<success_criteria>
- The repo has a boring, explicit prompt-eval entry point.
- All eval commands resolve locally through project dependencies.
- The directory structure is ready for Bouncer, Architect, and Conversationalist suites.
</success_criteria>

<output>
After completion, create `.planning/phases/10-promptfoo-evaluation-harness/10-01-SUMMARY.md`
</output>

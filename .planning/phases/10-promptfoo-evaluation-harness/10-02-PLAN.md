---
phase: 10-promptfoo-evaluation-harness
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - prompt-eval/bouncer/
  - src/lib/ai/prompts.ts
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "The first authoritative prompt-eval suite targets the Bouncer."
    - "The Bouncer suite uses a small hand-curated golden dataset instead of synthetic bulk cases."
    - "Duplicate leakage fails deterministically."
    - "The suite is structured so future soft-score judge assertions can be added without rewiring the root."
  artifacts:
    - path: "prompt-eval/bouncer/"
      provides: "First Golden Bouncer suite with fixtures, config, and assertions"
      exports: ["promptfooconfig.yaml", "fixtures", "assertions"]
    - path: "src/lib/ai/prompts.ts"
      provides: "Stable Bouncer prompt contract or exported prompt builder usable by the eval suite"
      exports: ["bouncer prompt surface"]
  key_links:
    - from: "prompt-eval/bouncer/promptfooconfig.yaml"
      to: "src/lib/ai/prompts.ts"
      via: "prompt-under-test"
      pattern: "bouncer"
    - from: "prompt-eval/bouncer/"
      to: "prompt-eval/shared/"
      via: "shared assertion helpers or provider conventions"
      pattern: "(shared|assert)"
---

<objective>
Write the first Golden evaluation suite for the AI Bouncer so the harness proves real product value instead of only existing as scaffolding.

Purpose: Make duplicate-prevention behavior measurable and failure-driven.

Output: Bouncer prompt-under-test, five curated cases, deterministic assertions, and a passing `eval:bouncer` command.
</objective>

<execution_context>
@/home/simone/.codex/get-shit-done/workflows/execute-plan.md
@/home/simone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-promptfoo-evaluation-harness/10-CONTEXT.md
@.planning/phases/10-promptfoo-evaluation-harness/10-RESEARCH.md
@.planning/phases/10-promptfoo-evaluation-harness/10-VALIDATION.md
@src/lib/ai/prompts.ts
@src/lib/ai/providers.ts

<tasks>

<task id="10-02-01" type="auto" tdd="true">
  <name>Task 1: Create the Golden Bouncer casuistry dataset</name>
  <files>prompt-eval/bouncer/</files>
  <action>
Create a small hand-curated fixture set with five brutal cases covering:
- exact duplicate
- near-synonym
- multilingual duplicate
- same concept, different phrasing
- clearly unrelated concept

Each case should encode the expected structural outcome clearly enough for deterministic assertions.
  </action>
  <acceptance_criteria>
    - the dataset contains five curated cases
    - each case is traceable to a real NeuroGraph duplicate-risk scenario
    - fixtures are stored under `prompt-eval/bouncer/`
  </acceptance_criteria>
</task>

<task id="10-02-02" type="auto" tdd="true">
  <name>Task 2: Wire promptfoo config and deterministic hard-fail assertions for the Bouncer</name>
  <files>prompt-eval/bouncer/, src/lib/ai/prompts.ts</files>
  <action>
Implement the first Bouncer suite so it can run through `promptfoo` locally.

Implementation rules:
- use a stable prompt-under-test from runtime code or an exported prompt builder
- enforce deterministic rejection/acceptance behavior
- fail immediately if a duplicate is wrongly allowed
- keep the output contract structured enough for later expansion into extraction assertions
- leave a clean seam for later judge-based scoring without requiring it now
  </action>
  <acceptance_criteria>
    - `npm run eval:bouncer` executes a real suite
    - duplicate cases fail if the model allows them
    - unrelated cases pass only when correctly allowed
    - the output contract is inspectable and predictable
  </acceptance_criteria>
</task>

<task id="10-02-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Human-check the first Golden suite for signal quality</name>
  <files>prompt-eval/bouncer/</files>
  <action>
Pause after the suite is green and confirm the five cases actually reflect NeuroGraph’s duplicate-prevention philosophy rather than generic toy examples.
  </action>
</task>

</tasks>

<verification>
Run the focused Bouncer suite:

```bash
cd /home/simone/projects/NeuroGraph
npm run eval:bouncer
npx tsc --noEmit
```
</verification>

<success_criteria>
- The first Golden suite exists and is worth trusting.
- Structural duplicate mistakes fail loudly.
- The harness already feels extensible to phases 11-13.
</success_criteria>

<output>
After completion, create `.planning/phases/10-promptfoo-evaluation-harness/10-02-SUMMARY.md`
</output>

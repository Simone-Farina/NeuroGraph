---
phase: 11-dag-manager-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/prompts.ts
  - src/lib/ai/architect.ts
  - src/lib/ai/__tests__/prompts.test.ts
  - src/lib/ai/__tests__/architect.test.ts
autonomous: true
requirements: [DAG-01, DAG-03]
---

<objective>
Define the Architect production contract: prompt text, strict Zod response schema, and local DAG invariant enforcement.

Purpose: make curriculum generation structurally trustworthy before any runtime route is introduced.

Output: an Architect prompt contract plus deterministic validation logic.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dag-manager-agent/11-CONTEXT.md
@src/lib/ai/prompts.ts

<tasks>

<task id="11-01-01" type="auto" tdd="true">
  <name>Task 1: Add the Architect system prompt and strict response schema</name>
  <files>src/lib/ai/prompts.ts, src/lib/ai/architect.ts</files>
  <action>
Define the Architect prompt contract and a strict schema with:
- `isValid`
- optional `refusalReason`
- `nodes`
- `synapses`

The schema must align to the requested NeuroGraph contract and forbid invented relation types.
  </action>
</task>

<task id="11-01-02" type="auto" tdd="true">
  <name>Task 2: Enforce local DAG invariants beyond basic JSON parsing</name>
  <files>src/lib/ai/architect.ts, src/lib/ai/__tests__/architect.test.ts</files>
  <action>
Add deterministic checks for:
- missing node references
- duplicate node titles
- duplicate synapses
- self-loops
- cycles across structural dependency edges

Silent repair is forbidden. Invalid graphs must fail the contract.
  </action>
</task>

<task id="11-01-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Confirm the contract shape before building the full Golden suite</name>
  <files>src/lib/ai/prompts.ts, src/lib/ai/architect.ts</files>
  <action>
Pause once the contract is stable enough that later promptfoo cases can target it without schema churn.
  </action>
</task>

</tasks>

<verification>
```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/lib/ai/__tests__/prompts.test.ts src/lib/ai/__tests__/architect.test.ts --reporter=verbose
npx tsc --noEmit
```
</verification>

<output>
After completion, create `.planning/phases/11-dag-manager-agent/11-01-SUMMARY.md`
</output>

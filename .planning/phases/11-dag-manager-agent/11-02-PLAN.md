---
phase: 11-dag-manager-agent
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - prompt-eval/architect/promptfooconfig.yaml
  - prompt-eval/architect/prompt.txt
  - prompt-eval/architect/cases.csv
  - prompt-eval/shared/architect-response.schema.json
  - prompt-eval/shared/neurograph-architect-provider.mjs
autonomous: true
requirements: [DAG-02, DAG-03]
---

<objective>
Create the first Golden Architect promptfoo suite with explicit valid curricula, cycle traps, and relation-boundary cases.

Purpose: prove the Architect contract survives real prompt-eval cases instead of only unit tests.

Output: a passing Architect suite under `prompt-eval/architect/`.
</objective>

<context>
@.planning/phases/11-dag-manager-agent/11-CONTEXT.md
@.planning/phases/11-dag-manager-agent/11-RESEARCH.md
@prompt-eval/README.md
@prompt-eval/shared/
@src/lib/ai/architect.ts

<tasks>

<task id="11-02-01" type="auto" tdd="true">
  <name>Task 1: Build the Golden dataset and provider wrapper</name>
  <files>prompt-eval/architect/cases.csv, prompt-eval/shared/neurograph-architect-provider.mjs</files>
  <action>
Author the hand-curated Architect cases:
- 3 valid target curricula
- 3 explicit cycle traps
- 2 `PREREQUISITE` vs `BUILDS_ON` boundary cases

Use the local provider wrapper so the eval harness reuses the runtime Architect prompt contract.
  </action>
</task>

<task id="11-02-02" type="auto" tdd="true">
  <name>Task 2: Add promptfoo config and schema assertions</name>
  <files>prompt-eval/architect/promptfooconfig.yaml, prompt-eval/shared/architect-response.schema.json</files>
  <action>
Wire promptfoo to assert:
- schema-valid JSON
- explicit refusal on cycle traps
- clean passes for valid cases

The suite should remain interpretable and local-first.
  </action>
</task>

<task id="11-02-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Confirm the Golden cases match NeuroGraph's pedagogy</name>
  <files>prompt-eval/architect/cases.csv</files>
  <action>
Pause after the suite passes so the case selection can be judged as product-meaningful rather than synthetic filler.
  </action>
</task>

</tasks>

<verification>
```bash
cd /home/simone/projects/NeuroGraph
npm run eval:architect
```
</verification>

<output>
After completion, create `.planning/phases/11-dag-manager-agent/11-02-SUMMARY.md`
</output>

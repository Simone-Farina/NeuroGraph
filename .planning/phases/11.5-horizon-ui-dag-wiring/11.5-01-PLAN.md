---
phase: 11.5-horizon-ui-dag-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/architect/route.ts
  - src/stores/graphStore.ts
  - src/lib/ai/architect.ts
autonomous: true
requirements: [HORIZON-01]
---

<objective>
Create the authenticated `/api/architect` runtime seam and the ephemeral store contract that holds ghost-path drafts in frontend state only.

Purpose: bridge the approved Architect contract into the product without polluting the persistent graph.

Output: strict route response plus draft-path state in the store.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dag-manager-agent/11-CONTEXT.md
@.planning/phases/11.5-horizon-ui-dag-wiring/11.5-CONTEXT.md
@src/lib/ai/architect.ts
@src/stores/graphStore.ts

<tasks>

<task id="11.5-01-01" type="auto" tdd="true">
  <name>Task 1: Add the authenticated `/api/architect` route</name>
  <files>src/app/api/architect/route.ts</files>
  <action>
Create a route that:
- authenticates the user
- accepts a target topic
- invokes the approved Architect contract
- returns the strict response schema

Implementation rules:
- no Supabase writes
- no draft persistence
- no silent schema reshaping beyond the approved contract
  </action>
</task>

<task id="11.5-01-02" type="auto" tdd="true">
  <name>Task 2: Extend store state for ephemeral ghost-path drafts</name>
  <files>src/stores/graphStore.ts</files>
  <action>
Add the minimal state needed for:
- current learning target
- active ghost draft nodes and edges
- active ghost node selection
- briefing-mode transitions

Implementation rules:
- keep persisted neurons and draft ghost paths separate
- keep the draft ephemeral and resettable
  </action>
</task>

<task id="11.5-01-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Confirm the route and store boundary before styling work begins</name>
  <files>src/app/api/architect/route.ts, src/stores/graphStore.ts</files>
  <action>
Pause after the route and state model are stable enough that the UI can consume them without reworking the contract.
  </action>
</task>

</tasks>

<verification>
```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/app/api/architect/__tests__/route.test.ts src/stores/__tests__/graphStore.test.ts --reporter=verbose
npx tsc --noEmit
```
</verification>

<output>
After completion, create `.planning/phases/11.5-horizon-ui-dag-wiring/11.5-01-SUMMARY.md`
</output>

---
phase: 11.5-horizon-ui-dag-wiring
plan: 02
type: execute
wave: 2
depends_on: [11.5-01]
files_modified:
  - src/components/graph/GraphPanel.tsx
  - src/components/graph/GhostNeuronNode.tsx
  - src/app/globals.css
autonomous: true
requirements: [HORIZON-02]
---

<objective>
Render the Architect draft inside the graph workspace with a graph-side target trigger and low-anxiety Fog of War styling.

Purpose: let users preview a curriculum as a blueprint before they commit to learning any part of it.

Output: `Set Learning Target` flow plus `ghostNeuron` rendering.
</objective>

<context>
@.planning/phases/11.5-horizon-ui-dag-wiring/11.5-CONTEXT.md
@.planning/phases/11.5-horizon-ui-dag-wiring/11.5-RESEARCH.md
@src/components/graph/GraphPanel.tsx
@src/stores/graphStore.ts
@src/app/(app)/layout.tsx

<tasks>

<task id="11.5-02-01" type="auto" tdd="true">
  <name>Task 1: Add the GraphPanel target trigger and draft-loading interaction</name>
  <files>src/components/graph/GraphPanel.tsx</files>
  <action>
Add a quiet `Set Learning Target` control in GraphPanel chrome that submits a target and loads the returned draft into the store.

Implementation rules:
- keep the interaction compact and elegant
- avoid a full-screen wizard or a large modal
- reuse existing shell presets rather than inventing new sizing behavior
  </action>
</task>

<task id="11.5-02-02" type="auto" tdd="true">
  <name>Task 2: Register and style the `ghostNeuron` node type</name>
  <files>src/components/graph/GhostNeuronNode.tsx, src/components/graph/GraphPanel.tsx, src/app/globals.css</files>
  <action>
Render Architect nodes as Fog of War blueprints with:
- dashed borders
- muted colors
- reduced opacity
- readable titles

Implementation rules:
- no markdown editor
- no persisted-neuron affordances
- clear visual difference from real neurons
  </action>
</task>

<task id="11.5-02-03" type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Confirm the ghost path reads as a blueprint, not an error state</name>
  <files>src/components/graph/GraphPanel.tsx, src/components/graph/GhostNeuronNode.tsx</files>
  <action>
Pause after the first visual pass so the graph styling can be judged against the Danish Computation contract before the briefing handoff is layered on.
  </action>
</task>

</tasks>

<verification>
```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/graph/__tests__/GraphPanel.horizon.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<output>
After completion, create `.planning/phases/11.5-horizon-ui-dag-wiring/11.5-02-SUMMARY.md`
</output>

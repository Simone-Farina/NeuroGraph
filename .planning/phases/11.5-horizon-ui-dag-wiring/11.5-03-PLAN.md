---
phase: 11.5-horizon-ui-dag-wiring
plan: 03
type: execute
wave: 3
depends_on: [11.5-01, 11.5-02]
files_modified:
  - src/stores/graphStore.ts
  - src/app/(app)/layout.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/graph/HorizonBriefingPanel.tsx
autonomous: true
requirements: [HORIZON-03]
---

<objective>
Bridge ghost-path inspection into active learning through a left-panel briefing mode and seeded chat handoff.

Purpose: turn a preview node into a deliberate learning session without persisting it as a neuron.

Output: ghost briefing plus `Start Learning (Crystallize)` conversation seed flow.
</objective>

<context>
@.planning/phases/11.5-horizon-ui-dag-wiring/11.5-CONTEXT.md
@.planning/phases/11.5-horizon-ui-dag-wiring/11.5-RESEARCH.md
@src/app/(app)/layout.tsx
@src/components/chat/ChatPanel.tsx
@src/components/chat/CrystallizeBootstrap.tsx
@src/stores/graphStore.ts

<tasks>

<task id="11.5-03-01" type="auto" tdd="true">
  <name>Task 1: Add left-panel briefing mode for ghost nodes</name>
  <files>src/stores/graphStore.ts, src/app/(app)/layout.tsx, src/components/graph/HorizonBriefingPanel.tsx</files>
  <action>
Introduce a briefing mode that shows:
- selected ghost node title
- generated definition
- quiet contextual copy
- primary `Start Learning (Crystallize)` CTA

Implementation rules:
- keep the panel editorial and calm
- do not turn it into a second graph editor
  </action>
</task>

<task id="11.5-03-02" type="auto" tdd="true">
  <name>Task 2: Reuse the chat seed pattern for the ghost-to-chat handoff</name>
  <files>src/components/chat/ChatPanel.tsx, src/stores/graphStore.ts</files>
  <action>
When the user chooses `Start Learning (Crystallize)`, open a fresh chat session seeded with the selected concept and definition, reusing the app's existing intent-driven seeded-chat pattern where possible.
  </action>
</task>

<task id="11.5-03-03" type="checkpoint:human-verify" gate="hard">
  <name>Task 3: Hard checkpoint on the full Horizon flow</name>
  <files>src/components/graph/GraphPanel.tsx, src/components/graph/HorizonBriefingPanel.tsx, src/components/chat/ChatPanel.tsx</files>
  <action>
Verify end to end:
- set a target
- inspect ghost nodes
- open briefing mode
- start learning in chat
- refresh and confirm the path was ephemeral
  </action>
</task>

</tasks>

<verification>
```bash
cd /home/simone/projects/NeuroGraph
npx vitest run src/components/graph/__tests__/GraphPanel.horizon.test.tsx src/components/chat/__tests__/ChatPanel.horizon.test.tsx --reporter=verbose
npx tsc --noEmit
```
</verification>

<output>
After completion, create `.planning/phases/11.5-horizon-ui-dag-wiring/11.5-03-SUMMARY.md`
</output>

---
phase: 15-ui-ux-polish-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/GraphPanel.tsx
  - src/components/layout/AppSidebar.tsx
autonomous: true
requirements: [BUG-05, BUG-08]

must_haves:
  truths:
    - "The Set Learning Target UI uses the app's editorial design language — no white pill buttons or mismatched border styles"
    - "The API key in the sidebar is masked after its initial one-time reveal — only visible at the moment of generation"
  artifacts:
    - path: "src/components/graph/GraphPanel.tsx"
      provides: "Horizon controls restyled with rounded-xl, border-white/5-8, font-serif — both empty-state and populated-state copies"
      contains: "rounded-xl"
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "API key masking with isKeyPrefixVisible state and 10s auto-hide timer"
      contains: "ng_****"
  key_links:
    - from: "src/components/graph/GraphPanel.tsx"
      to: ".impeccable.md"
      via: "Design tokens: rounded-xl, border-white/5, font-serif"
      pattern: "rounded-xl.*border-white"
    - from: "src/components/layout/AppSidebar.tsx"
      to: "setTimeout"
      via: "10-second auto-mask timer with useRef cleanup"
      pattern: "setTimeout.*setIsKeyPrefixVisible"
---

<objective>
Redesign the "Set Learning Target" horizon controls to match the app's editorial design language (BUG-05), and add API key masking in the sidebar with a 10-second reveal window after generation (BUG-08).

Purpose: The horizon controls currently use a HUD-pill aesthetic that clashes with the dark editorial world of the rest of the app. The API key prefix is permanently visible when it should be masked for security.
Output: Two components restyled/enhanced to production design quality.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ui-ux-polish-security/15-CONTEXT.md
@.planning/phases/15-ui-ux-polish-security/15-RESEARCH.md

<interfaces>
<!-- Design tokens from canonical editorial components (from .impeccable.md and source reads) -->

Editorial design language tokens:
- Containers: `rounded-2xl border border-white/5 bg-white/[0.02]` or `bg-neural-dark/80`
- Buttons (interactive): `rounded-xl border border-white/[0.08]` hover `border-white/[0.2]`
- Text tiers: `text-white/30` (muted) → `text-white/40` → `text-white/60` → `text-white/80` → `text-white/90`
- Typography: `font-serif` for content labels, headings; geometric sans for small UI
- Praised reference: NeurogenesisSuggestion.tsx — `pl-5 border-l-2 border-white/10`, `font-serif text-2xl` titles
- No decorative color — color reserved for semantic learning state (decay, mastery, activity)

From src/components/layout/AppSidebar.tsx:
- `keyState` FSM: 'idle' | 'generating' | 'revealed' | 'has-key' | 'error'
- `keyPrefix` state: string holding first chars of generated key
- `handleGenerate()`: creates key, transitions to 'revealed', then to 'has-key'
- Line ~328: `has-key` state always renders `{keyPrefix}...` — this is what needs masking
- Existing `copied` state uses a setTimeout pattern for auto-reset — similar pattern for key masking

From src/components/graph/GraphPanel.tsx:
- Two identical copies of horizon controls:
  - Empty state branch (~lines 330-373): rendered when `!flowNodes.length`
  - Populated state branch (~lines 380-419): overlaid on the graph canvas
- Both use: `rounded-[24px]` container, `rounded-full` buttons, `border-white/12`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Learning Target horizon controls to editorial design language (BUG-05)</name>
  <files>src/components/graph/GraphPanel.tsx</files>
  <read_first>
    - src/components/graph/GraphPanel.tsx (full file — identify BOTH horizon controls locations: empty state ~line 330 and populated state ~line 380)
    - .impeccable.md (full file — canonical design tokens and aesthetic rules)
    - src/components/chat/NeurogenesisSuggestion.tsx (lines 1-50 — praised design reference for editorial styling)
    - src/components/graph/ReviewMode.tsx (lines 1-50 — review panel design reference)
  </read_first>
  <action>
**BUG-05 (per D-02, D-03, D-04):** Restyle BOTH copies of the horizon controls in `GraphPanel.tsx` to match the dark editorial design language. Every modified element must meet production design quality — this is not a functional patch.

**Container changes (both locations):**
- Replace `rounded-[24px]` with `rounded-2xl` — rectangular card, not a floating pill
- Replace `border-white/7` (or similar) with `border-white/5` — subtler, editorial muted border
- Update background to `bg-neural-dark/80` with `backdrop-blur-xl` — consistent with app chrome

**Button changes (both locations):**
- Replace `rounded-full` with `rounded-xl` on all buttons (Set Learning Target, Generate, Cancel)
- Replace `border-white/12` with `border-white/8` — matches the editorial border tier
- Replace `bg-white/[0.05]` or `bg-white/[0.06]` with `bg-white/[0.03]` — more muted
- Add `font-serif` to button label text — consistent with graph chrome typography
- Update text color from bright values to `text-white/60` default, `hover:text-white/80`
- Add transition: `transition hover:border-white/15 hover:bg-white/[0.06]`

**Input field changes (both locations):**
- Replace `rounded-full` with `rounded-xl`
- Replace `border-white/10` with `border-white/5`
- Add `bg-white/[0.03]` background
- Add `focus:border-white/12` focus state

**Label text:** Add `font-serif` to "Set Learning Target" label and any descriptive text — consistent with neuron card and chat headers.

**CRITICAL:** Both the empty-state copy (~line 330) and the populated-state copy (~line 380) MUST receive identical styling changes. Consider extracting into a shared `<HorizonControls>` sub-component to prevent future drift (Claude's discretion — extraction is preferred for DRY but parallel edits satisfy the requirement).

**Design reference:** The result should feel like it belongs in the same dark editorial world as the review panel and the chat empty state — muted, deliberate, no floating HUD element. Monochrome only — no decorative color.
  </action>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && npx vitest run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both horizon control locations use rounded-xl/rounded-2xl instead of rounded-full/rounded-[24px]
    - Both locations use border-white/5 or border-white/8 instead of border-white/12
    - Both locations use font-serif for label and button text
    - No rounded-full classes remain on any horizon control element
    - No border-white/12 classes remain on any horizon control element
    - Both copies are visually identical (or extracted into a shared component)
    - Existing test suite passes
  </acceptance_criteria>
  <done>Horizon controls match the app's dark editorial aesthetic — muted borders, serif typography, rectangular buttons; both copies are consistent; tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add API key masking with auto-expire reveal (BUG-08)</name>
  <files>src/components/layout/AppSidebar.tsx</files>
  <read_first>
    - src/components/layout/AppSidebar.tsx (full file — understand keyState FSM, handleGenerate flow, and has-key render section around line 328)
  </read_first>
  <action>
**BUG-08 (per D-07):** Replace the always-visible `{keyPrefix}...` display with a masked `ng_****...` default. Only show the actual prefix for 10 seconds immediately after key generation, then auto-mask.

**State additions (near existing state declarations):**
```typescript
const [isKeyPrefixVisible, setIsKeyPrefixVisible] = useState(false);
const keyPrefixTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```

**In handleGenerate (after the line that sets keyState to 'revealed' or transitions to 'has-key'):**
```typescript
setIsKeyPrefixVisible(true);
if (keyPrefixTimerRef.current) clearTimeout(keyPrefixTimerRef.current);
keyPrefixTimerRef.current = setTimeout(() => setIsKeyPrefixVisible(false), 10_000);
```

**Timer cleanup (add a useEffect):**
```typescript
useEffect(() => {
  return () => {
    if (keyPrefixTimerRef.current) clearTimeout(keyPrefixTimerRef.current);
  };
}, []);
```

**In the `has-key` render section (around line 328):** Replace the always-visible prefix display:
```tsx
<span className="font-mono text-xs text-white/50 truncate">
  {isKeyPrefixVisible ? `${keyPrefix}...` : 'ng_****...'}
</span>
```

**Design quality (per D-04):** The masked display `ng_****...` should use the same `font-mono text-xs text-white/50` styling as the revealed prefix to avoid layout shift. Use a subtle framer-motion crossfade between states if it fits naturally with the existing sidebar AnimatePresence patterns (Claude's discretion). The transition should feel deliberate and editorial — a quiet fade, not a flashy animation.

**Import additions:** Add `useRef` to the React import if not already present.
  </action>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && npx vitest run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AppSidebar has isKeyPrefixVisible state defaulting to false
    - handleGenerate sets isKeyPrefixVisible to true with a 10-second auto-reset timer
    - Timer is cleaned up via useRef + useEffect cleanup pattern
    - has-key render shows 'ng_****...' by default and keyPrefix only when isKeyPrefixVisible is true
    - No layout shift between masked and unmasked states (same styling classes)
    - Existing test suite passes
  </acceptance_criteria>
  <done>API key prefix is masked by default in sidebar; only visible for 10 seconds after generation; timer properly cleaned up; tests green</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=dot` — full test suite passes
- `grep -n "rounded-full" src/components/graph/GraphPanel.tsx` — should return NO matches in horizon controls sections
- `grep -n "ng_\\*\\*\\*\\*" src/components/layout/AppSidebar.tsx` — shows masked display string
- `grep -n "isKeyPrefixVisible" src/components/layout/AppSidebar.tsx` — shows state, timer, and render usage
</verification>

<success_criteria>
1. Learning Target controls look editorial — muted borders, serif type, rectangular buttons (BUG-05 closed)
2. API key masked as ng_****... by default, briefly revealed on generation (BUG-08 closed)
3. Full Vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-ux-polish-security/15-02-SUMMARY.md`
</output>

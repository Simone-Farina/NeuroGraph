---
phase: 19-enterprise-prompt-engineering
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - prompt-eval/conversationalist/cases.yaml
  - prompt-eval/conversationalist/promptfooconfig.yaml
  - prompt-eval/architect/cases.csv
  - prompt-eval/architect/promptfooconfig.yaml
autonomous: true
requirements:
  - PROMPT-04

must_haves:
  truths:
    - "The eval suite contains 42+ total cases across all agent suites"
    - "Conversationalist suite has behavioral cases for mistake handling, calibrated difficulty, meta-questioning, and multi-turn neurogenesis priming"
    - "Architect suite has boundary cases for comprehension test disambiguation (PREREQUISITE vs RELATED vs BUILDS_ON)"
    - "Judge model version is pinned in all promptfoo configs"
  artifacts:
    - path: "prompt-eval/conversationalist/cases.yaml"
      provides: "4 new behavioral golden cases"
      min_lines: 240
    - path: "prompt-eval/architect/cases.csv"
      provides: "4 new boundary golden cases"
      min_lines: 12
    - path: "prompt-eval/conversationalist/promptfooconfig.yaml"
      provides: "Pinned judge model version"
      contains: "gpt-4o-2024-08-06"
    - path: "prompt-eval/architect/promptfooconfig.yaml"
      provides: "Pinned judge model version"
      contains: "gpt-4o-2024-08-06"
  key_links:
    - from: "prompt-eval/conversationalist/cases.yaml"
      to: "src/lib/ai/prompts.ts"
      via: "eval provider extracts CHAT_SYSTEM_PROMPT"
      pattern: "CHAT_SYSTEM_PROMPT"
    - from: "prompt-eval/architect/cases.csv"
      to: "src/lib/ai/architect.ts"
      via: "eval provider uses architectResponseSchema"
      pattern: "architectResponseSchema"
---

<objective>
Expand the promptfoo eval suite from 34 to 42+ cases with behavioral assertions that validate the new Khanmigo patterns and comprehension test heuristic.

Purpose: Prompt changes from Plan 01 need regression coverage. Without behavioral eval cases, drift in the new patterns (mistake handling, calibrated difficulty, meta-questioning) would go undetected. The judge model must be pinned to prevent score drift from silent model updates.

Output: 4 new conversationalist cases (behavioral), 4 new architect cases (boundary disambiguation), pinned judge model in all configs. Total: 42+ cases.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-enterprise-prompt-engineering/19-CONTEXT.md
@.planning/phases/19-enterprise-prompt-engineering/19-01-SUMMARY.md

@prompt-eval/conversationalist/cases.yaml
@prompt-eval/conversationalist/promptfooconfig.yaml
@prompt-eval/architect/cases.csv
@prompt-eval/architect/promptfooconfig.yaml
@prompt-eval/bouncer/promptfooconfig.yaml

<interfaces>
<!-- Eval infrastructure patterns the executor must follow -->

From prompt-eval/conversationalist/cases.yaml (case format):
```yaml
- vars:
    topic: "Example Topic"
    user_message: "The user's message"
    expected_neurogenesis: false  # or true
    multi_turn: # optional, for multi-turn scenarios
      - role: user
        content: "First message"
      - role: assistant
        content: "Response"
      - role: user
        content: "Follow-up"
  assert:
    - type: javascript
      value: |
        // Custom assertion logic
```

From prompt-eval/architect/cases.csv (case format):
```csv
description,target_topic,concepts,constraints,expected_is_valid,required_synapse,expected_refusal_fragment
```

From prompt-eval/conversationalist/promptfooconfig.yaml:
```yaml
# Default assertions include:
# 1. Socratic tone score >= 0.8 (javascript assertion)
# 2. Neurogenesis triggered/withheld check (javascript assertion)
# Judge model is NOT currently pinned
```

From prompt-eval/architect/promptfooconfig.yaml:
```yaml
# Default assertions include:
# 1. is-json schema validation
# 2. isValid matches expected_is_valid
# 3. Synapse source/target titles match node titles
# 4. Refusal structure check
# 5. Required synapse check
# 6. Cycle detection in eval output
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin judge model and add 4 conversationalist behavioral cases (PROMPT-04)</name>
  <files>prompt-eval/conversationalist/cases.yaml, prompt-eval/conversationalist/promptfooconfig.yaml, prompt-eval/bouncer/promptfooconfig.yaml</files>
  <read_first>
    - prompt-eval/conversationalist/cases.yaml (full file — understand existing case format and scenarios)
    - prompt-eval/conversationalist/promptfooconfig.yaml (full file — see current assertion structure)
    - prompt-eval/bouncer/promptfooconfig.yaml (check if judge model needs pinning here too)
    - prompt-eval/shared/neurograph-conversationalist-provider.mjs (understand how cases are consumed)
    - .planning/phases/19-enterprise-prompt-engineering/19-01-SUMMARY.md (confirm what prompt changes were made)
  </read_first>
  <action>
**Pin judge model** in `prompt-eval/conversationalist/promptfooconfig.yaml` and `prompt-eval/bouncer/promptfooconfig.yaml`:
Add to any `llm-rubric` assertion configs (if present) or add a top-level `evaluateOptions` block:
```yaml
evaluateOptions:
  maxConcurrency: 2
```
If there are `llm-rubric` assertions, ensure they specify: `provider: openai:gpt-4o-2024-08-06`. If the configs currently use heuristic-only assertions (no llm-rubric), add a comment at the top: `# Judge model pin: openai:gpt-4o-2024-08-06 — re-baseline scores if changed`.

**Add 4 new conversationalist behavioral cases** to `prompt-eval/conversationalist/cases.yaml`. Each case targets a specific Khanmigo pattern from PROMPT-01. Follow the existing case format exactly.

Case 1 — **Mistake handling**: User states an incorrect fact confidently ("Photosynthesis produces carbon dioxide"). The agent MUST NOT correct directly. Expected: response contains a question about the user's reasoning ("how did you arrive at that" or equivalent), NOT the correct answer.

Case 2 — **Calibrated difficulty**: User gives a vague, uncertain response ("I think it's something about electrons?"). The agent MUST simplify and offer a concrete anchor, not escalate. Expected: response breaks down the concept to a simpler level, does not assume understanding.

Case 3 — **Meta-questioning**: After 3+ turns of substantive exchange on a topic, the agent SHOULD occasionally surface assumptions. Expected: response contains a meta-question about reasoning process or assumptions ("what assumption" or "what would have to be true").

Case 4 — **Multi-turn neurogenesis priming**: A multi-turn conversation where the user progressively demonstrates Analyze-level depth. By the final turn, the user articulates a genuine cross-domain insight. Expected: `expected_neurogenesis: true` and the response acknowledges the insight depth.

Each case should have custom `assert` blocks with `type: javascript` assertions that check for the specific behavioral pattern (e.g., checking response does NOT contain the direct correction, checking response contains a question mark, etc.). Follow the existing assertion patterns in the file.
  </action>
  <acceptance_criteria>
    - promptfooconfig.yaml (conversationalist) contains "gpt-4o-2024-08-06" as a comment or provider pin
    - bouncer promptfooconfig.yaml contains matching judge model pin
    - cases.yaml contains 4 new cases (17 total conversationalist cases)
    - New cases cover: mistake handling, calibrated difficulty, meta-questioning, neurogenesis priming
    - Each new case has at least one custom javascript assertion
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && grep -c "^-" prompt-eval/conversationalist/cases.yaml | xargs test 17 -le && grep -q "gpt-4o-2024-08-06" prompt-eval/conversationalist/promptfooconfig.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 new conversationalist behavioral cases added. Judge model pinned. Total conversationalist cases: 17.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 architect boundary cases (PROMPT-04)</name>
  <files>prompt-eval/architect/cases.csv, prompt-eval/architect/promptfooconfig.yaml</files>
  <read_first>
    - prompt-eval/architect/cases.csv (full file — understand existing CSV format and scenarios)
    - prompt-eval/architect/promptfooconfig.yaml (full file — see assertion structure)
    - .planning/phases/19-enterprise-prompt-engineering/19-01-SUMMARY.md (confirm inferPrerequisites and architect changes)
  </read_first>
  <action>
**Pin judge model** in `prompt-eval/architect/promptfooconfig.yaml`: Add comment `# Judge model pin: openai:gpt-4o-2024-08-06 — re-baseline scores if changed`.

**Add 4 new architect boundary cases** to `prompt-eval/architect/cases.csv` that test the comprehension test disambiguation from PROMPT-02. Follow the existing CSV format exactly: `description,target_topic,concepts,constraints,expected_is_valid,required_synapse,expected_refusal_fragment`

Case 5 — **RELATED not PREREQUISITE (parallel languages)**: Target "JavaScript", concepts "Python|JavaScript". Expected valid, but the synapse (if any) should be RELATED not PREREQUISITE. Add a `required_synapse` of `Python|JavaScript|RELATED` to ensure the model does not create a false prerequisite.

Case 6 — **BUILDS_ON not PREREQUISITE (enrichment, not gating)**: Target "Design Patterns", concepts "Object-Oriented Programming|Design Patterns|SOLID Principles". Expected valid, with `Design Patterns|Object-Oriented Programming|BUILDS_ON` as required synapse.

Case 7 — **Comprehension test: strong prerequisite**: Target "Gradient Descent", concepts "Calculus|Partial Derivatives|Gradient Descent". Expected valid, with `Partial Derivatives|Gradient Descent|PREREQUISITE` as required synapse.

Case 8 — **No connection (unrelated domains)**: Target "Quantum Computing", concepts "Quantum Computing|Italian Cuisine". Expected valid, NO synapse between them. Use an assertion to verify the model does not create a PREREQUISITE between these unrelated concepts.

These 4 cases bring architect from 8 to 12 cases. Combined: 17 conversationalist + 12 architect + 13 bouncer = 42 total.
  </action>
  <acceptance_criteria>
    - architect promptfooconfig.yaml contains "gpt-4o-2024-08-06" as a comment or provider pin
    - cases.csv contains 12 data rows (header + 12 cases)
    - New cases cover: RELATED vs PREREQUISITE disambiguation, BUILDS_ON boundary, strong prerequisite, no-connection
    - CSV format matches existing cases exactly (same column count and quoting)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && wc -l < prompt-eval/architect/cases.csv | xargs test 12 -le && grep -q "gpt-4o-2024-08-06" prompt-eval/architect/promptfooconfig.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 new architect boundary cases added. Judge model pinned. Total across all suites: 42+ cases (17 + 12 + 13).</done>
</task>

</tasks>

<verification>
Total case count validation:
```bash
echo "Conversationalist:" && grep -c "^-" prompt-eval/conversationalist/cases.yaml
echo "Architect:" && echo $(($(wc -l < prompt-eval/architect/cases.csv) - 1))
echo "Bouncer:" && echo $(($(wc -l < prompt-eval/bouncer/cases.csv) - 1))
```
Expected: 17 + 12 + 13 = 42 total.

Judge model pinned in all configs:
```bash
grep -l "gpt-4o-2024-08-06" prompt-eval/*/promptfooconfig.yaml
```
Expected: conversationalist, architect, bouncer configs all contain the pin.
</verification>

<success_criteria>
- Eval suite total is 42+ cases (up from 34)
- 4 new conversationalist cases cover: mistake handling, calibrated difficulty, meta-questioning, multi-turn neurogenesis priming
- 4 new architect cases cover: RELATED vs PREREQUISITE, BUILDS_ON boundary, strong prerequisite, no-connection
- Judge model version pinned as `openai:gpt-4o-2024-08-06` in all 3 promptfoo configs
- All cases follow existing format conventions
</success_criteria>

<output>
After completion, create `.planning/phases/19-enterprise-prompt-engineering/19-02-SUMMARY.md`
</output>

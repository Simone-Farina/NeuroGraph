---
phase: 23-pure-conversationalist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompt-eval/conversationalist/cases.yaml
  - prompt-eval/conversationalist/promptfooconfig.yaml
  - prompt-eval/shared/neurograph-conversationalist-provider.mjs
  - supabase/migrations/20260325000000_truncate_chat_tables.sql
autonomous: true
requirements: [EVAL-01, AGENT-06]

must_haves:
  truths:
    - "Conversationalist eval suite has 4+ new golden cases testing no-jargon, natural-flow, depth-challenge, and mistake-handling"
    - "Eval assertions FAIL if response contains banned jargon (Neuron, Crystallize, Bloom, Taxonomy, Insight)"
    - "Eval assertions FAIL if response contains unsolicited bullet points"
    - "Eval assertions PASS only if response contains a follow-up question"
    - "DB migration truncates messages and conversations tables"
  artifacts:
    - path: "prompt-eval/conversationalist/cases.yaml"
      provides: "New golden cases for pure conversationalist eval"
      contains: "no-jargon"
    - path: "prompt-eval/conversationalist/promptfooconfig.yaml"
      provides: "Updated eval config without neurogenesis assertions"
      contains: "javascript"
    - path: "prompt-eval/shared/neurograph-conversationalist-provider.mjs"
      provides: "Updated eval provider without tool-calling"
      contains: "scoreSocraticTone"
    - path: "supabase/migrations/20260325000000_truncate_chat_tables.sql"
      provides: "Destructive chat data reset migration"
      contains: "TRUNCATE"
  key_links:
    - from: "prompt-eval/conversationalist/promptfooconfig.yaml"
      to: "prompt-eval/conversationalist/cases.yaml"
      via: "tests: cases.yaml reference"
      pattern: "tests.*cases\\.yaml"
    - from: "prompt-eval/conversationalist/promptfooconfig.yaml"
      to: "prompt-eval/shared/neurograph-conversationalist-provider.mjs"
      via: "provider exec reference"
      pattern: "exec:node.*neurograph-conversationalist-provider"
---

<objective>
Rebuild the conversationalist eval suite from scratch for the pure-text-only Socratic tutor contract, and create the DB migration that hard-resets all existing chat data.

Purpose: Eval-driven development — the eval suite defines the behavioral contract BEFORE the prompt is rewritten. The DB migration eliminates legacy tool-call messages that would corrupt the new architecture.
Output: 4 new golden eval cases with jargon/bullet/Socratic assertions, updated eval provider (no tools), and a TRUNCATE migration.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-pure-conversationalist/23-CONTEXT.md

<interfaces>
<!-- Eval provider reads CHAT_SYSTEM_PROMPT from prompts.ts via regex extraction -->

From prompt-eval/conversationalist/promptfooconfig.yaml:
```yaml
providers:
  - exec:node ../shared/neurograph-conversationalist-provider.mjs
defaultTest:
  assert:
    - type: javascript  # Socratic tone assertion
    - type: javascript  # Neurogenesis assertion
tests: cases.yaml
```

From prompt-eval/shared/neurograph-conversationalist-provider.mjs:
```javascript
function scoreSocraticTone(text) { /* returns 0-1 float */ }
function detectsAnalyzeLevel(text) { /* returns boolean */ }
function extractChatPrompt() { /* reads CHAT_SYSTEM_PROMPT from src/lib/ai/prompts.ts */ }
// Current provider passes tools: { suggest_neurogenesis: {...} } to generateText
// Current provider returns { response, socratic_score, neurogenesis_triggered }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild conversationalist eval suite with pure-text assertions</name>
  <files>
    prompt-eval/conversationalist/cases.yaml,
    prompt-eval/conversationalist/promptfooconfig.yaml,
    prompt-eval/shared/neurograph-conversationalist-provider.mjs
  </files>
  <read_first>
    prompt-eval/conversationalist/cases.yaml,
    prompt-eval/conversationalist/promptfooconfig.yaml,
    prompt-eval/shared/neurograph-conversationalist-provider.mjs,
    .planning/phases/23-pure-conversationalist/23-CONTEXT.md
  </read_first>
  <action>
**1. Replace cases.yaml entirely** with 4 new golden cases (per CONTEXT.md decisions). Remove ALL 17 old cases. The new cases:

**Case 1: "No jargon in response"**
- vars.messages: one prior turn about learning recursion
- vars.final_user_message: "I think each recursive call just reduces the problem."
- No `expected_neurogenesis` field (concept removed)
- assert block: JavaScript regex FAIL if response contains ANY of: "Neuron", "Crystallize", "Bloom", "Taxonomy", "Insight", "Knowledge Graph", "Neurogenesis" (case-insensitive, word-boundary)

**Case 2: "Natural flow — no unsolicited bullet points"**
- vars.messages: two prior turns about database design
- vars.final_user_message: "I'm not sure when to normalize versus denormalize."
- assert block: JavaScript regex FAIL if response matches `^\s*[\*\-\d\.]+\s+` on any line (unsolicited bullets/lists). Use `.split('\n')` and test each line.

**Case 3: "Depth challenge — pushes user to think deeper"**
- vars.messages: three prior turns about machine learning, user giving shallow answers
- vars.final_user_message: "Neural networks just learn patterns from data."
- assert block: JavaScript PASS only if response contains at least one question mark AND response length > 100 chars (must push deeper, not just acknowledge)

**Case 4: "Mistake handling — no direct correction"**
- vars.messages: one prior turn about photosynthesis
- vars.final_user_message: "Photosynthesis produces carbon dioxide — plants breathe it out."
- assert block: JavaScript FAIL if response starts with "Actually" or "That's incorrect" (case-insensitive). PASS only if response contains a question mark (must probe reasoning).

All 4 cases must also pass the Socratic tone default assertion (score >= 0.7 — lowered from 0.8 because the new prompt is more natural and may score differently on the existing heuristic).

**2. Rewrite promptfooconfig.yaml:**
- Remove the old neurogenesis default assertion entirely (no more `neurogenesis_triggered` checks)
- Keep the Socratic tone default assertion but lower threshold to >= 0.7:
  ```yaml
  defaultTest:
    assert:
      - type: javascript
        value: |
          const result = JSON.parse(output);
          const score = result.socratic_score;
          return { pass: score >= 0.7, score, reason: `Socratic tone score: ${score.toFixed(2)}` };
  ```
- Keep `tests: cases.yaml`
- Keep `providers: - exec:node ../shared/neurograph-conversationalist-provider.mjs`
- Keep `prompts: - file://prompt.json`

**3. Update neurograph-conversationalist-provider.mjs:**
- **Remove** `neurogenesisJsonSchema` object entirely
- **Remove** `BLOOM_ANALYZE_SIGNALS` array and `BLOOM_QUESTION_EXEMPTION` regex
- **Remove** `detectsAnalyzeLevel()` function
- **Remove** the `tools: { suggest_neurogenesis: {...} }` object from the `generateText` call (lines ~298-310)
- **Remove** `neurogenesisTriggered` computation and the `neurogenesis_triggered` field from the output JSON
- The `generateText` call becomes a simple text-only call: `generateText({ model, system: systemPrompt, messages: buildMessages(vars), temperature: 0, maxOutputTokens: 600 })`
- Output JSON becomes: `{ response: text, socratic_score: scoreSocraticTone(text) }`
- **Update** `heuristicConversationalist()` to also return only `{ response, socratic_score }` (remove `neurogenesis_triggered`)
- **Keep** `scoreSocraticTone()`, `extractChatPrompt()`, `buildMessages()`, `resolveModel()`, `hasKeyFor()`, `parseJson()` — all still needed
- **Keep** all teaching signals in `scoreSocraticTone` — these still score the new natural prompt
  </action>
  <verify>
    <automated>grep -c "Neuron\|Crystallize\|Bloom\|Taxonomy\|Insight" prompt-eval/conversationalist/cases.yaml | head -1 && grep -c "neurogenesis_triggered" prompt-eval/shared/neurograph-conversationalist-provider.mjs && grep -c "suggest_neurogenesis" prompt-eval/shared/neurograph-conversationalist-provider.mjs && grep -c "no-jargon\|natural-flow\|depth-challenge\|mistake-handling" prompt-eval/conversationalist/cases.yaml</automated>
  </verify>
  <acceptance_criteria>
    - grep "neurogenesis_triggered" in provider.mjs returns 0 matches
    - grep "suggest_neurogenesis" in provider.mjs returns 0 matches
    - grep "no-jargon\|natural-flow\|depth-challenge\|mistake-handling" in cases.yaml returns 4+ matches (one per case description)
    - cases.yaml has exactly 4 test cases (no old cases remain)
    - promptfooconfig.yaml has no neurogenesis assertion in defaultTest
    - Provider output format is { response, socratic_score } only
  </acceptance_criteria>
  <done>4 new golden eval cases exist with jargon/bullet/Socratic/mistake assertions; provider calls generateText without tools; neurogenesis concept completely removed from eval suite</done>
</task>

<task type="auto">
  <name>Task 2: Create TRUNCATE migration for messages and conversations tables</name>
  <files>supabase/migrations/20260325000000_truncate_chat_tables.sql</files>
  <read_first>
    supabase/migrations/20260324000000_cleanup_legacy_related_synapses.sql,
    .planning/phases/23-pure-conversationalist/23-CONTEXT.md
  </read_first>
  <action>
Create `supabase/migrations/20260325000000_truncate_chat_tables.sql` with the following content (per CONTEXT.md decision: HARD RESET, sole user beta, data is disposable):

```sql
-- Phase 23: Pure Conversationalist — hard reset chat data
-- Removes all legacy tool-call messages and conversations to prevent
-- rehydration errors in the new tool-free chat architecture.
-- This is a destructive migration — acceptable because NeuroGraph is
-- single-user beta with disposable conversation data.

TRUNCATE TABLE messages CASCADE;
TRUNCATE TABLE conversations CASCADE;
```

The `CASCADE` ensures any foreign key references are handled. Messages depends on conversations via foreign key, so truncating conversations CASCADE will also clear messages, but explicitly truncating both makes the intent clear.

Do NOT create any complex JSON-parsing migration — clean slate per user decision.
  </action>
  <verify>
    <automated>cat supabase/migrations/20260325000000_truncate_chat_tables.sql | grep -c "TRUNCATE"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260325000000_truncate_chat_tables.sql
    - File contains "TRUNCATE TABLE messages CASCADE"
    - File contains "TRUNCATE TABLE conversations CASCADE"
    - File contains NO "ALTER", "UPDATE", or "jsonb" operations (no complex migration)
  </acceptance_criteria>
  <done>Migration file exists that will truncate messages and conversations tables, eliminating all legacy tool-call data</done>
</task>

</tasks>

<verification>
- `grep -rn "neurogenesis_triggered" prompt-eval/` returns 0 matches
- `grep -rn "suggest_neurogenesis" prompt-eval/shared/` returns 0 matches
- `wc -l prompt-eval/conversationalist/cases.yaml` shows a reasonable file (not empty, not the old 365-line file)
- `cat supabase/migrations/20260325000000_truncate_chat_tables.sql` shows TRUNCATE statements
- `node prompt-eval/shared/neurograph-conversationalist-provider.mjs "" "{}" "{\"vars\":{\"messages\":[],\"final_user_message\":\"test\"}}"` returns JSON with only `response` and `socratic_score` fields (no `neurogenesis_triggered`)
</verification>

<success_criteria>
- Conversationalist eval suite has 4 new golden cases with strict no-jargon, no-bullet, Socratic, and mistake-handling assertions
- Eval provider generates text without tool-calling capability
- DB migration ready to truncate all chat data
- No references to neurogenesis, Bloom classification, or tool-calling remain in the eval suite
</success_criteria>

<output>
After completion, create `.planning/phases/23-pure-conversationalist/23-01-SUMMARY.md`
</output>

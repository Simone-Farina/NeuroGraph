---
phase: 24-silent-observer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prompt-eval/bloom-evaluator/promptfooconfig.yaml
  - prompt-eval/bloom-evaluator/cases.yaml
  - prompt-eval/bloom-evaluator/prompt.txt
  - prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs
autonomous: true
requirements: [EVAL-02, EVAL-03]

must_haves:
  truths:
    - "Bloom evaluator eval suite has 6 golden cases covering Remember/Understand, Analyze, and Evaluate/Create tiers"
    - "Cases below threshold (Remember/Understand) are correctly classified as below Analyze"
    - "Cases at or above threshold (Analyze/Evaluate/Create) are correctly classified as Analyze or higher"
    - "Eval suite can run offline with heuristic fallback (no live API key required in CI)"
    - "Eval suite exists BEFORE any production bloom evaluator code ships"
  artifacts:
    - path: "prompt-eval/bloom-evaluator/promptfooconfig.yaml"
      provides: "Promptfoo configuration for bloom evaluator eval suite"
      contains: "neurograph-bloom-evaluator-provider"
    - path: "prompt-eval/bloom-evaluator/cases.yaml"
      provides: "6 golden eval cases with JavaScript assertion blocks"
      min_lines: 80
    - path: "prompt-eval/bloom-evaluator/prompt.txt"
      provides: "Bloom evaluator system prompt for eval and production use"
      contains: "bloom_level"
    - path: "prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs"
      provides: "Eval provider that calls generateObject with bloom evaluator prompt and returns classification"
      contains: "generateObject"
  key_links:
    - from: "prompt-eval/bloom-evaluator/promptfooconfig.yaml"
      to: "prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs"
      via: "exec:node provider reference"
      pattern: "neurograph-bloom-evaluator-provider"
    - from: "prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs"
      to: "prompt-eval/bloom-evaluator/prompt.txt"
      via: "file read for system prompt"
      pattern: "prompt\\.txt"
---

<objective>
Create the Bloom Evaluator eval suite with 6 golden cases, the evaluator system prompt, and the promptfoo provider — establishing the behavioral contract BEFORE any production code ships.

Purpose: Eval-driven development (EVAL-03) requires the test suite to exist first so it gates the production implementation in Plan 02. The eval suite validates that a cheap LLM can distinguish Remember/Understand from Analyze/Evaluate/Create with a 0.75 confidence threshold.

Output: Complete promptfoo eval suite under `prompt-eval/bloom-evaluator/` with provider, prompt, config, and 6 assertion-backed golden cases.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-silent-observer/24-CONTEXT.md

<interfaces>
<!-- Existing eval infrastructure patterns to follow exactly -->

From prompt-eval/conversationalist/promptfooconfig.yaml:
```yaml
description: NeuroGraph Golden Conversationalist suite
prompts:
  - file://prompt.json
providers:
  - exec:node ../shared/neurograph-conversationalist-provider.mjs
defaultTest:
  assert:
    - type: javascript
      value: |
        // assertion block
tests: cases.yaml
```

From prompt-eval/shared/neurograph-conversationalist-provider.mjs:
```javascript
// Pattern: resolveModel() checks env vars, falls back to openai:gpt-4o-mini
// heuristicConversationalist(vars) provides offline fallback
// buildMessages(vars) converts vars.messages + vars.final_user_message
// main() calls generateText or heuristic, writes JSON to stdout
```

From src/lib/ai/providers.ts:
```typescript
export type ModelRole = 'chat' | 'synthesis_fast' | 'neurogenesis_heavy' | 'evaluator';
export function getModelForRole(role: ModelRole) { ... }
// 'evaluator' role defaults to 'openai:gpt-4o-mini'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bloom evaluator system prompt and promptfoo provider</name>
  <files>
    prompt-eval/bloom-evaluator/prompt.txt
    prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs
  </files>
  <read_first>
    - prompt-eval/shared/neurograph-conversationalist-provider.mjs (provider pattern to follow)
    - prompt-eval/conversationalist/promptfooconfig.yaml (config pattern)
    - src/lib/ai/providers.ts (model resolution pattern)
  </read_first>
  <action>
    Create `prompt-eval/bloom-evaluator/prompt.txt` containing the Bloom Evaluator system prompt. The prompt must:
    - Instruct the LLM to classify the cognitive depth of the LAST 3 user messages in a conversation
    - Require chain-of-thought reasoning BEFORE classification (per CONTEXT.md: "forces LLM to reason before classifying, prevents hallucinated classifications")
    - Use Bloom's Taxonomy levels: Remember, Understand, Apply, Analyze, Evaluate, Create
    - Output schema: `{ reasoning: string, bloom_level: string, confidence: number }` where confidence is 0.0-1.0
    - Include clear boundary descriptions between levels, especially the Understand/Analyze boundary (the critical threshold)
    - Emphasize that user messages asking factual recall questions or paraphrasing are Remember/Understand, while messages comparing tradeoffs, dissecting mechanisms, or proposing novel ideas are Analyze+

    Create `prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs` following the exact pattern from `neurograph-conversationalist-provider.mjs`:
    - resolveModel(): Check PROMPTFOO_BLOOM_EVALUATOR_MODEL, then AI_MODEL_EVALUATOR, then fallback to 'google:gemini-2.5-flash' (per CONTEXT.md: Gemini Flash for cost/speed). Include hasKeyFor() check for google (GOOGLE_API_KEY, GOOGLE_GENERATIVE_AI_API_KEY, GEMINI_API_KEY).
    - Use `generateObject` (NOT generateText) with a zod-like schema for `{ reasoning: string, bloom_level: string, confidence: number }`. Import from 'ai' package. Since this is a .mjs file outside the project build, use the `output: 'no-schema'` mode with a JSON schema in the prompt instead, and parse the JSON response manually. Alternatively, use `generateText` with a JSON-mode prompt and parse the output.
    - buildMessages(vars): Construct the evaluator input from `vars.messages` (array of {role, content}) — extract the last 3 user messages as the evaluation target
    - extractBloomPrompt(): Read prompt.txt from the bloom-evaluator directory using `path.resolve(process.cwd(), 'prompt-eval/bloom-evaluator/prompt.txt')`
    - heuristicBloomEvaluator(vars): Offline fallback that uses keyword-based heuristic:
      - Check last user messages for Analyze+ signals: "compare", "tradeoff", "mechanism", "why does", "what if", "evaluate", "design", "create", "propose"
      - If 2+ signals found: return `{ reasoning: "Heuristic: multiple analytical signals detected", bloom_level: "Analyze", confidence: 0.80 }`
      - Otherwise: return `{ reasoning: "Heuristic: surface-level discourse", bloom_level: "Understand", confidence: 0.85 }`
    - main(): Call generateText with system prompt + user messages, parse JSON from response, fall back to heuristic on error. Write JSON to stdout.
  </action>
  <verify>
    <automated>test -f prompt-eval/bloom-evaluator/prompt.txt && test -f prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs && node -e "import('file://' + process.cwd() + '/prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs').catch(e => { console.error(e); process.exit(1); })" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - prompt-eval/bloom-evaluator/prompt.txt exists and contains "bloom_level" and "reasoning" and "confidence"
    - prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs exists and contains "generateText" or "generateObject"
    - Provider imports from 'ai' package and '@ai-sdk/google' or '@ai-sdk/openai'
    - Provider has heuristicBloomEvaluator function for offline CI fallback
    - Provider reads prompt.txt via fs.readFileSync
    - Provider outputs JSON with { reasoning, bloom_level, confidence } shape
  </acceptance_criteria>
  <done>Bloom evaluator system prompt and promptfoo provider exist, provider is syntactically valid Node.js ESM</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 golden eval cases and promptfoo config</name>
  <files>
    prompt-eval/bloom-evaluator/cases.yaml
    prompt-eval/bloom-evaluator/promptfooconfig.yaml
  </files>
  <read_first>
    - prompt-eval/conversationalist/cases.yaml (case structure pattern)
    - prompt-eval/conversationalist/promptfooconfig.yaml (config pattern)
    - prompt-eval/bloom-evaluator/prompt.txt (just created in Task 1)
    - prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs (just created in Task 1)
  </read_first>
  <action>
    Create `prompt-eval/bloom-evaluator/promptfooconfig.yaml`:
    ```yaml
    description: NeuroGraph Golden Bloom Evaluator suite — Phase 24 cognitive classification contract
    prompts:
      - file://prompt.txt
    providers:
      - exec:node ../shared/neurograph-bloom-evaluator-provider.mjs
    tests: cases.yaml
    ```
    No defaultTest needed — each case has its own assertion block.

    Create `prompt-eval/bloom-evaluator/cases.yaml` with exactly 6 golden cases (per CONTEXT.md decision). Use JavaScript assertion blocks in each case. Each case provides `vars.messages` (a multi-turn conversation array with 3+ user messages) and asserts on the parsed JSON output:

    **Cases 1-2: Remember/Understand (must score BELOW threshold)**
    - Case 1 "factual-recall": User messages are pure recall — "What is photosynthesis?", "It converts sunlight to energy", "Plants use chlorophyll". Assert: `bloom_level` is "Remember" or "Understand", confidence > 0.5. FAIL if bloom_level is "Analyze", "Evaluate", or "Create".
    - Case 2 "paraphrase-comprehension": User messages rephrase textbook content — "TCP uses a three-way handshake", "The SYN packet initiates", "Then the server responds with SYN-ACK". Assert: `bloom_level` is "Remember" or "Understand". FAIL if Analyze+.

    **Cases 3-4: Analyze (must score ABOVE threshold)**
    - Case 3 "tradeoff-analysis": User messages compare and contrast — "SQL vs NoSQL depends on the access patterns", "Normalization reduces redundancy but increases join complexity", "The tradeoff shifts when read-heavy workloads dominate". Assert: `bloom_level` is "Analyze", "Evaluate", or "Create". FAIL if "Remember" or "Understand". Assert: confidence >= 0.75.
    - Case 4 "mechanism-dissection": User messages dissect how something works — "The garbage collector marks reachable objects first", "Then it sweeps unreachable ones, but the pause time depends on heap size", "What if we used generational collection to reduce full-GC frequency?". Assert: `bloom_level` is "Analyze" or higher. Confidence >= 0.75.

    **Cases 5-6: Evaluate/Create (must score well above threshold)**
    - Case 5 "critical-evaluation": User messages judge and critique — "Microservices solve team autonomy but introduce distributed system complexity", "The network partition tolerance tradeoff means we sacrifice either consistency or availability", "I'd argue that for a solo developer, a modular monolith is strictly better because the coordination cost exceeds the deployment flexibility benefit". Assert: `bloom_level` is "Evaluate" or "Create". Confidence >= 0.80.
    - Case 6 "novel-synthesis": User messages propose something new — "What if we combined spaced repetition with knowledge graphs?", "The graph structure could weight edges by retention decay", "You could auto-generate review sessions that traverse weak prerequisite chains first". Assert: `bloom_level` is "Create" or "Evaluate". Confidence >= 0.80.

    Each assertion block must:
    - Parse output as JSON: `const result = JSON.parse(output);`
    - Check `result.bloom_level` against allowed/forbidden values
    - Check `result.confidence` against thresholds
    - Return `{ pass, score, reason }` with descriptive reason strings
    - Include a structural check that `result.reasoning` is a non-empty string (chain-of-thought enforcement)
  </action>
  <verify>
    <automated>grep -c "description:" prompt-eval/bloom-evaluator/cases.yaml | grep -q "^6$" && grep -q "neurograph-bloom-evaluator-provider" prompt-eval/bloom-evaluator/promptfooconfig.yaml && echo "PASS: 6 cases found, config references provider" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - prompt-eval/bloom-evaluator/cases.yaml contains exactly 6 test cases (6 "description:" lines)
    - Each case has a vars.messages array with at least 3 user messages
    - Each case has at least one JavaScript assertion block
    - Cases 1-2 assert bloom_level is Remember or Understand (FAIL on Analyze+)
    - Cases 3-4 assert bloom_level is Analyze or higher with confidence >= 0.75
    - Cases 5-6 assert bloom_level is Evaluate or Create with confidence >= 0.80
    - Every assertion checks that reasoning is a non-empty string
    - promptfooconfig.yaml references cases.yaml and the bloom-evaluator provider
  </acceptance_criteria>
  <done>6 golden eval cases exist with JavaScript assertion blocks, config wired to provider, suite is ready to run with `npx promptfoo eval -c prompt-eval/bloom-evaluator/promptfooconfig.yaml`</done>
</task>

</tasks>

<verification>
1. `ls prompt-eval/bloom-evaluator/` shows prompt.txt, cases.yaml, promptfooconfig.yaml
2. `ls prompt-eval/shared/neurograph-bloom-evaluator-provider.mjs` exists
3. `grep -c "description:" prompt-eval/bloom-evaluator/cases.yaml` returns 6
4. Provider file is valid ESM (no syntax errors on import check)
5. No production code modified — eval suite is standalone
</verification>

<success_criteria>
- Bloom evaluator eval suite with 6 golden cases exists under prompt-eval/bloom-evaluator/
- Provider handles both live (Gemini Flash) and offline (heuristic) modes
- System prompt enforces chain-of-thought reasoning before classification
- Cases cover the full Bloom spectrum: 2 below threshold, 2 at threshold, 2 above
- All assertions use JavaScript blocks with explicit pass/fail/reason
- Zero production files touched — eval gates implementation in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/24-silent-observer/24-01-SUMMARY.md`
</output>

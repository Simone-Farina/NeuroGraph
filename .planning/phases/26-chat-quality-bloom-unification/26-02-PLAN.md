---
phase: 26-chat-quality-bloom-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/prompts.ts
  - prompt-eval/conversationalist/cases.yaml
autonomous: true
requirements:
  - CHAT-02
  - BLOOM-01

must_haves:
  truths:
    - "The Conversationalist responds with 1-2 paragraphs on short conversational turns, not a rigid 3-paragraph structure"
    - "AI responses vary their opening style — not always starting the same way"
    - "AI responses usually close with a question but occasionally use a brief observation instead"
    - "The Phase 21 client-side Bloom keyword heuristic is confirmed absent from the codebase"
    - "The sole Bloom classification source is the Phase 24 LLM evaluator at POST /api/bloom-evaluate"
    - "Stale worktree branches containing the never-merged Phase 21 heuristic code are cleaned up"
  artifacts:
    - path: "src/lib/ai/prompts.ts"
      provides: "Rewritten CHAT_SYSTEM_PROMPT with shorter-default response length"
      contains: "one tight paragraph is often enough"
    - path: "prompt-eval/conversationalist/cases.yaml"
      provides: "Paragraph-count regression assertion"
      contains: "response-length"
  key_links:
    - from: "prompt-eval/conversationalist/cases.yaml"
      to: "src/lib/ai/prompts.ts"
      via: "promptfoo eval exercises the CHAT_SYSTEM_PROMPT"
      pattern: "paragraphs"
    - from: "src/app/api/bloom-evaluate/route.ts"
      to: "src/stores/graphStore.ts"
      via: "Sole Bloom classification path — no client-side heuristic exists"
      pattern: "bloom-evaluate"
---

<objective>
Calibrate the Conversationalist's response length to default to 1-2 paragraphs (not 3), add a promptfoo regression assertion for paragraph count, and verify + clean up the Bloom unification status.

Purpose: The current CHAT_SYSTEM_PROMPT anchors the model to 3 paragraphs every turn, making responses feel lecture-like. The prompt rewrite defaults to shorter, sharper turns. The Bloom cleanup formally verifies that the Phase 21 heuristic was never merged and removes the stale worktree artifacts.

Output: Modified prompts.ts with shorter-default prompt. New eval case in cases.yaml. Verified Bloom unification. Cleaned worktrees.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-chat-quality-bloom-unification/26-CONTEXT.md
@.planning/phases/26-chat-quality-bloom-unification/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/ai/prompts.ts (current CHAT_SYSTEM_PROMPT — the full text to rewrite):
```typescript
export const CHAT_SYSTEM_PROMPT = `You are NeuroGraph, a rigorous Socratic thinking companion. You are here to think alongside the user — not to lecture, not to hand out answers, but to help them reason more clearly and reach understanding on their own.

## Your Approach

Respond in flowing prose. One to three paragraphs per turn. Never produce bullet points or numbered lists unless the user explicitly asks for one. Avoid starting every reply the same way — vary your openings naturally, as a thoughtful colleague would. When you have something to add, add it; when the user's thinking opens an interesting angle, explore it. Your job is not to fill space but to advance the thinking.

Build on what was just said rather than restating it. Bring in a related idea, a counterexample, a historical parallel, or a different framing when it genuinely illuminates the topic — not as a mandatory step, but as a natural contribution. Then close with exactly one focused question. Vary the question type across turns: sometimes push for precision, sometimes challenge an assumption, sometimes ask the user to compare or explain consequences.

When the conversation includes a ## Relevant Knowledge Context section, weave those ideas in naturally where they connect. When a ## Reference Catalog section is present, reference established concepts to link new thinking to what the user already knows.

## Depth Encouragement
...
## Pedagogical Calibration
...
## Constraints

- Never produce bullet points or numbered lists unless the user explicitly requests one
- Respond in flowing prose — one to three paragraphs
- Always close with exactly one focused follow-up question
- When the conversation includes a ## Relevant Knowledge Context or ## Reference Catalog section, reference it naturally — do not quote it verbatim
`;
```

From prompt-eval/conversationalist/promptfooconfig.yaml:
```yaml
description: NeuroGraph Golden Conversationalist suite — Phase 23 pure-text contract
prompts:
  - file://prompt.json
providers:
  - exec:node ../shared/neurograph-conversationalist-provider.mjs
defaultTest:
  assert:
    - type: javascript
      value: |
        const result = JSON.parse(output);
        const score = result.socratic_score;
        return { pass: score >= 0.7, score, reason: `Socratic tone score: ${score.toFixed(2)}` };
tests: cases.yaml
```

From prompt-eval/conversationalist/cases.yaml (4 existing cases):
- Case 1: no-jargon
- Case 2: natural-flow
- Case 3: depth-challenge
- Case 4: mistake-handling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paragraph-count eval assertion to conversationalist cases (eval-first per D-11)</name>
  <files>prompt-eval/conversationalist/cases.yaml</files>
  <read_first>
    - prompt-eval/conversationalist/cases.yaml
    - prompt-eval/conversationalist/promptfooconfig.yaml
    - prompt-eval/shared/neurograph-conversationalist-provider.mjs
    - .planning/phases/26-chat-quality-bloom-unification/26-RESEARCH.md
  </read_first>
  <action>
Add a 5th test case to `prompt-eval/conversationalist/cases.yaml` that asserts the model does NOT always produce 3+ paragraphs. This case is written BEFORE the prompt change (eval-first approach per D-11) to establish a baseline.

Append the following case at the end of the file:

```yaml
# ──────────────────────────────────────────────────────────────────────────────
# Case 5: Response length — shorter default (Phase 26 CHAT-02 regression guard)
# ──────────────────────────────────────────────────────────────────────────────

- description: "response-length — model should produce 1-2 paragraphs on a short conversational turn, not always 3+"
  vars:
    messages:
      - role: user
        content: "What is a closure in JavaScript?"
      - role: assistant
        content: "Good question to dig into. When you hear the word 'closure,' what picture comes to mind — is it about functions, variables, or something else entirely?"
    final_user_message: "So it's like the function remembers its outer scope?"
  assert:
    - type: javascript
      value: |
        // Paragraph count check: count \n\n-separated blocks (standard paragraph delimiter).
        // FAIL if response has 3+ paragraphs — this short conversational turn should elicit
        // a 1-2 paragraph reply. The provider caps output at 600 tokens, which comfortably
        // fits even a 3-paragraph response, so truncation is not a concern here.
        const result = JSON.parse(output);
        const text = result.response.trim();
        const paragraphs = text.split(/\n\n+/).filter(p => p.trim().length > 0);
        if (paragraphs.length >= 3) {
          return {
            pass: false,
            score: 0,
            reason: `Response has ${paragraphs.length} paragraphs — expected 1-2 for a short conversational turn`
          };
        }
        return { pass: true, score: 1, reason: `Response has ${paragraphs.length} paragraph(s) — matches shorter-default expectation` };
```

Important: The YAML case must maintain the same indentation style as the existing 4 cases. Each case starts with `- description:` at column 0. The `assert` block uses 4-space indentation. The JavaScript `value:` block uses YAML `|` literal block scalar.
  </action>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && node -e "const yaml = require('js-yaml'); const fs = require('fs'); const cases = yaml.load(fs.readFileSync('prompt-eval/conversationalist/cases.yaml', 'utf8')); console.log('Total cases:', cases.length); const last = cases[cases.length-1]; console.log('Last case:', last.description); console.log('Has assert:', !!last.assert);"</automated>
  </verify>
  <acceptance_criteria>
    - cases.yaml contains exactly 5 test cases (parseable by js-yaml)
    - The 5th case has description containing "response-length"
    - The 5th case vars include `final_user_message: "So it's like the function remembers its outer scope?"`
    - The 5th case assert block contains `paragraphs.length >= 3`
    - The 5th case assert block contains `split(/\\n\\n+/)`
    - cases.yaml is valid YAML (node js-yaml parse succeeds)
  </acceptance_criteria>
  <done>A new paragraph-count eval case exists in cases.yaml that will FAIL if the model always produces 3+ paragraph responses on short turns</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CHAT_SYSTEM_PROMPT for shorter, varied responses (per D-07 through D-10)</name>
  <files>src/lib/ai/prompts.ts</files>
  <read_first>
    - src/lib/ai/prompts.ts
    - .planning/phases/26-chat-quality-bloom-unification/26-CONTEXT.md
    - .planning/phases/26-chat-quality-bloom-unification/26-RESEARCH.md
    - .planning/phases/26-chat-quality-bloom-unification/26-UI-SPEC.md
  </read_first>
  <action>
Modify `CHAT_SYSTEM_PROMPT` in `src/lib/ai/prompts.ts`. Only the prompt string changes — no code, imports, or exports are modified (per D-07).

**Change 1 — Response length (D-08):**
In the `## Your Approach` section, replace:
```
One to three paragraphs per turn.
```
With:
```
Keep responses concise. One tight paragraph is often enough. Add a second only when genuinely needed. Reserve a third for rare cases requiring real elaboration.
```

**Change 2 — Opening variety (D-09):**
In the `## Your Approach` section, after the sentence `Avoid starting every reply the same way — vary your openings naturally, as a thoughtful colleague would.`, add:
```
Never start two consecutive replies the same way. Vary between building on the user's point, introducing a new angle, or asking directly.
```

**Change 3 — Closing format (D-10):**
In the `## Your Approach` section, replace:
```
Then close with exactly one focused question.
```
With:
```
Usually close with a question, but occasionally a brief observation or reframe is enough.
```

**Change 4 — Constraints section (D-08, D-10):**
In the `## Constraints` section at the bottom, replace:
```
- Respond in flowing prose — one to three paragraphs
- Always close with exactly one focused follow-up question
```
With:
```
- Respond in flowing prose — one to two paragraphs by default, three only when the topic demands it
- Usually close with a question; occasionally a brief observation or reframe suffices
```

**What NOT to change:**
- The opening sentence ("You are NeuroGraph, a rigorous Socratic thinking companion...")
- The `## Depth Encouragement` section
- The `## Pedagogical Calibration` section
- The bullet-point ban constraint
- The knowledge context constraint
- The `BOUNCER_SYSTEM_PROMPT`, `ARCHITECT_SYSTEM_PROMPT`, and `MAX_CONTEXT_MESSAGES` exports
  </action>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && npx tsc --noEmit 2>&1 | head -10 && grep -c "one tight paragraph is often enough" src/lib/ai/prompts.ts && grep -c "One to three paragraphs" src/lib/ai/prompts.ts && grep -c "Always close with exactly one" src/lib/ai/prompts.ts</automated>
  </verify>
  <acceptance_criteria>
    - prompts.ts contains the string "one tight paragraph is often enough" (D-08 replacement)
    - prompts.ts does NOT contain "One to three paragraphs per turn" (old text removed)
    - prompts.ts contains "Never start two consecutive replies the same way" (D-09 addition)
    - prompts.ts contains "Usually close with a question, but occasionally a brief observation or reframe is enough" (D-10 replacement)
    - prompts.ts does NOT contain "Always close with exactly one focused follow-up question" (old text removed)
    - prompts.ts Constraints section contains "one to two paragraphs by default"
    - prompts.ts Constraints section contains "Usually close with a question; occasionally"
    - prompts.ts still contains "You are NeuroGraph, a rigorous Socratic thinking companion" (opening unchanged)
    - prompts.ts still exports BOUNCER_SYSTEM_PROMPT, ARCHITECT_SYSTEM_PROMPT, MAX_CONTEXT_MESSAGES (unchanged)
    - TypeScript compiles without errors
    - grep for "One to three paragraphs" returns count 0
    - grep for "Always close with exactly one" returns count 0
  </acceptance_criteria>
  <done>
    - CHAT_SYSTEM_PROMPT defaults to 1-2 paragraphs (not 3)
    - Opening variety instruction added
    - Closing format loosened from rigid question requirement to flexible question/observation
    - Constraints section updated to match body instructions
    - No other exports or code changed
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Bloom unification and clean up stale worktree (per D-12 through D-15)</name>
  <files></files>
  <read_first>
    - src/components/chat/GenerateNeuronButton.tsx
    - src/app/api/bloom-evaluate/route.ts
    - src/stores/graphStore.ts
    - .planning/phases/26-chat-quality-bloom-unification/26-CONTEXT.md
  </read_first>
  <action>
This task verifies BLOOM-01 is satisfied (Phase 21 heuristic never merged) and cleans up stale worktree artifacts.

**Step 1 — Verify heuristic absence (D-13):**
Run `grep -r "classifyBloomLevel\|BLOOM_ANALYZE_SIGNALS\|BloomDepthMeter" src/` and confirm it returns no output. This proves the Phase 21 client-side heuristic was never merged to the develop branch.

**Step 2 — Verify sole classification source (D-12, D-15):**
Confirm that `src/app/api/bloom-evaluate/route.ts` exists and is the only Bloom classification endpoint. Confirm that `src/stores/graphStore.ts` contains `bloomLevel`, `bloomConfidence`, and `isBloomPending` state fields driven by the LLM evaluator. Confirm that `GenerateNeuronButton.tsx` reads from the Zustand store (not from any client-side heuristic).

**Step 3 — Clean up stale worktree (D-14):**
Run the following commands:
```bash
# Remove the Phase 21 heuristic worktree
git worktree remove .claude/worktrees/agent-ac5685ca --force

# Delete the local branch
git branch -D worktree-agent-ac5685ca 2>/dev/null || true

# Check if branch was pushed to remote and delete if so
git ls-remote --heads origin worktree-agent-ac5685ca
# If output is non-empty: git push origin --delete worktree-agent-ac5685ca
```

**Scope limit:** Only clean up `worktree-agent-ac5685ca` — the other worktrees are from different phases and are out of scope.

**Step 4 — Document verification:**
The SUMMARY for this plan will record the Bloom verification results. No code changes are needed — BLOOM-01 was pre-satisfied.
  </action>
  <verify>
    <automated>cd /Users/simone/Desktop/Projects/NeuroGraph && grep -r "classifyBloomLevel\|BLOOM_ANALYZE_SIGNALS\|BloomDepthMeter" src/ | wc -l | tr -d ' ' && echo "expect 0" && test ! -d .claude/worktrees/agent-ac5685ca && echo "worktree removed" || echo "worktree still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "classifyBloomLevel|BLOOM_ANALYZE_SIGNALS|BloomDepthMeter" src/` returns 0 matches
    - The directory `.claude/worktrees/agent-ac5685ca` no longer exists
    - `git worktree list` does NOT contain `agent-ac5685ca`
    - `src/app/api/bloom-evaluate/route.ts` exists (sole classification source)
    - `GenerateNeuronButton.tsx` imports from `@/stores/graphStore` (not from any heuristic module)
  </acceptance_criteria>
  <done>
    - Phase 21 client-side Bloom heuristic confirmed absent from codebase (grep returns 0)
    - Sole Bloom classification source is POST /api/bloom-evaluate (Phase 24 LLM evaluator)
    - Stale worktree agent-ac5685ca removed
    - BLOOM-01 requirement formally verified and documented
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "classifyBloomLevel\|BLOOM_ANALYZE_SIGNALS\|BloomDepthMeter" src/` returns no output
3. `git worktree list` does not include `agent-ac5685ca`
4. `prompt-eval/conversationalist/cases.yaml` parses as valid YAML with 5 cases
5. `grep "One to three paragraphs" src/lib/ai/prompts.ts` returns no matches
6. `grep "Always close with exactly one" src/lib/ai/prompts.ts` returns no matches
7. `grep "one tight paragraph is often enough" src/lib/ai/prompts.ts` returns 1 match
</verification>

<success_criteria>
- CHAT_SYSTEM_PROMPT defaults to 1-2 paragraphs with varied openings and flexible closings
- Paragraph-count eval case exists and will catch regression to 3-paragraph default
- Bloom unification verified: no client-side heuristic in codebase, only LLM evaluator
- Stale Phase 21 worktree cleaned up
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-chat-quality-bloom-unification/26-02-SUMMARY.md`
</output>

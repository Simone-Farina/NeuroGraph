---
phase: 999.1-bloom-keyword-highlighting-animation
plan: 01
subsystem: ui
tags: [zustand, bloom-evaluator, tailwind, chat-panel, graphstore]

# Dependency graph
requires:
  - phase: 24-silent-observer
    provides: bloom-evaluate API route and triggerBloomEval in ChatPanel
  - phase: 25-decoupled-architect-pipeline
    provides: graphStore with bloomLevel/bloomConfidence state

provides:
  - Bloom evaluator API returns key_phrases alongside bloom_level, confidence, reasoning
  - graphStore holds bloomKeyPhrases string[] and latestBloomMessageId string | null
  - setBloomEval accepts optional keyPhrases and messageId params with safe defaults
  - resetBloomEval clears bloomKeyPhrases and latestBloomMessageId
  - ChatPanel triggerBloomEval wires key_phrases and latest user message ID to store
  - Tailwind safelist prevents purging of 6 Bloom highlight bg classes

affects: [999.1-02, bloom-keyword-rendering, chat-panel-highlighting]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - key_phrases extracted from LLM response with safe Array.isArray filter and string type guard
    - latestBloomMessageId captured from messagesRef at eval result time (after assistant message lands)
    - Tailwind safelist for dynamically-constructed class names

key-files:
  created: []
  modified:
    - src/app/api/bloom-evaluate/route.ts
    - src/stores/graphStore.ts
    - src/stores/__tests__/graphStore.test.ts
    - tailwind.config.ts
    - src/components/chat/ChatPanel.tsx

key-decisions:
  - "999.1-01-key-phrases-verbatim: Prompt instructs LLM to return verbatim substrings from user messages, not paraphrased — enables exact text match for highlighting in Plan 02"
  - "999.1-01-safelist-opacity: /10 opacity for cool-toned Bloom levels (Remember/Understand/Apply), /15 for warm-toned (Analyze/Evaluate/Create) — per UI-SPEC color table"
  - "999.1-01-messageId-timing: latestUserMessageId captured from messagesRef.current at eval result arrival time — messagesRef is closure-safe and contains the correct user message as last in filtered list"
  - "999.1-01-safe-defaults: key_phrases and messageId have default params ([], null) in setBloomEval — maintains backward compat with all existing callers"

patterns-established:
  - "Bloom eval pipeline: API route extracts LLM field → store stores typed state → ChatPanel wires through"
  - "Graceful degradation: all error/fallback response paths in bloom-evaluate route include key_phrases: []"

requirements-completed: []

# Metrics
duration: 8min
completed: 2026-04-03
---

# Phase 999.1 Plan 01: Bloom Keyword Pipeline Foundation Summary

**Bloom evaluator data pipeline extended end-to-end: LLM extracts key_phrases, API returns them, Zustand stores them with latestBloomMessageId, ChatPanel wires them through, and Tailwind safelist protects 6 highlight classes from purging.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-03T02:43:00Z
- **Completed:** 2026-04-03T02:46:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Extended BLOOM_EVALUATOR_PROMPT with key_phrases instruction (1-4 verbatim substrings, 2-6 words each)
- API response parser safely extracts key_phrases with Array.isArray guard and string type filter
- All graceful degradation paths include key_phrases: [] to guarantee contract shape
- Zustand GraphStore extended with bloomKeyPhrases: string[] and latestBloomMessageId: string | null
- setBloomEval signature updated with optional params (backward compatible — all existing callers unaffected)
- resetBloomEval clears both new fields
- 4 new graphStore bloom state tests added and passing
- ChatPanel triggerBloomEval captures latestUserMessageId from messagesRef and forwards key_phrases to store
- Tailwind safelist added with 6 bg classes (bg-sky-400/10 through bg-yellow-300/15)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend Bloom evaluator API + Zustand store + Tailwind safelist (TDD)** - `70a8b6c` (feat)
2. **Task 2: Wire ChatPanel triggerBloomEval to pass key_phrases and message ID** - `987cb27` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/app/api/bloom-evaluate/route.ts` - key_phrases in prompt, parser, and all response paths
- `src/stores/graphStore.ts` - bloomKeyPhrases + latestBloomMessageId state and actions
- `src/stores/__tests__/graphStore.test.ts` - 4 new bloom state tests
- `tailwind.config.ts` - safelist with 6 Bloom highlight bg classes
- `src/components/chat/ChatPanel.tsx` - triggerBloomEval passes key_phrases and messageId to store

## Decisions Made
- key_phrases are verbatim substrings (not paraphrased) so Plan 02 can do exact substring highlighting
- latestUserMessageId captured at eval result arrival (messagesRef pattern, closure-safe, same as before)
- Optional params with defaults ([], null) preserve backward compatibility with all existing setBloomEval callers
- Tailwind safelist opacity follows UI-SPEC: /10 for cool-toned levels, /15 for warm-toned levels

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

**Pre-existing test failure (out of scope):** `graphStore horizon state > stores a valid horizon draft as ghost nodes and edges` expects `shellPreset: 'graph_zenith'` after `setHorizonDraft`, but `setHorizonDraft` in graphStore.ts does not set `shellPreset`. This predated this plan — logged in deferred-items.md.

**Pre-existing TypeScript errors (out of scope):** 3 TS errors in `architect.test.ts`, `inferPrerequisites.test.ts`, and `providers.ts` predated this plan — logged in deferred-items.md.

## Known Stubs

None - all data pipeline wiring is complete. Plan 02 will consume bloomKeyPhrases and latestBloomMessageId for UI rendering.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Full data pipeline complete: bloom_level + key_phrases + latestBloomMessageId flow from LLM to Zustand store
- Plan 02 can read bloomKeyPhrases and latestBloomMessageId from store to apply highlight marks in ChatPanel messages
- Tailwind safelist ensures dynamic bg classes survive purging

---
*Phase: 999.1-bloom-keyword-highlighting-animation*
*Completed: 2026-04-03*

---
phase: 999.1-bloom-keyword-highlighting-animation
plan: 02
subsystem: ui
tags: [react, zustand, tailwind, vitest, promptfoo, bloom-evaluator, message-list, highlight-animation]

# Dependency graph
requires:
  - phase: 999.1-01
    provides: bloomKeyPhrases, latestBloomMessageId in graphStore, Tailwind safelist for 6 Bloom bg classes

provides:
  - renderHighlightedText exported function for case-insensitive regex split highlight rendering
  - bloomToHighlightClass exported utility mapping 6 Bloom levels to bg tint classes
  - useBloomHighlight exported hook with isHighlighted state and 5s auto-fade setTimeout
  - MessageList renders keyword highlights on latest user message only (D-07 guard)
  - 11 unit tests covering all highlight behaviors and timing
  - promptfoo eval cases validate key_phrases as non-empty string array on all 6 golden cases
  - prompt.txt synced with key_phrases JSON field instruction

affects: [999.1-visual-verification, bloom-keyword-feature-complete]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - TDD red-green cycle for React hook with fake timers (vi.useFakeTimers + renderHook)
    - Capturing-group regex split for substring highlighting (odd indices are matches)
    - useEffect cleanup (clearTimeout) prevents timer memory leak on unmount/re-trigger
    - motion-reduce:transition-none respects prefers-reduced-motion accessibility

key-files:
  created:
    - src/components/chat/__tests__/MessageList.test.tsx
  modified:
    - src/components/chat/MessageList.tsx
    - prompt-eval/bloom-evaluator/cases.yaml
    - prompt-eval/bloom-evaluator/prompt.txt

key-decisions:
  - "999.1-02-capturing-group-split: String.split() with capturing-group regex puts matches at odd indices — avoids lastIndex drift pitfall from RESEARCH.md"
  - "999.1-02-local-highlight-state: isHighlighted is local component state not Zustand — transient animation state belongs in component, not global store"
  - "999.1-02-motion-reduce: motion-reduce:transition-none applied to highlight spans — prefers-reduced-motion users see instant transitions, not animated ones"
  - "999.1-02-shouldHighlight-guard: message.id === latestBloomMessageId check inside map — previous messages and all assistant messages never receive highlight spans"

patterns-established:
  - "TDD red-green with fake timers: vitest fake timers + renderHook + act() pattern for testing hooks with setTimeout"
  - "Bloom keyword highlighting pipeline: store.bloomKeyPhrases → useBloomHighlight → renderHighlightedText → MessageList user message span"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-04-03
---

# Phase 999.1 Plan 02: Keyword Highlight Rendering in MessageList Summary

**MessageList extended with Bloom keyword highlighting: case-insensitive regex split renders verbatim key phrases as soft background tint spans on the latest user message only, fading in over 300ms and auto-clearing after 5 seconds.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-03T02:52:00Z
- **Completed:** 2026-04-03T03:04:00Z (Tasks 1-3; Task 4 is pending visual verification)
- **Tasks:** 3 of 4 (Task 4 is checkpoint:human-verify)
- **Files modified:** 4

## Accomplishments

### Task 1 (TDD Red): MessageList highlight unit tests
- Created `src/components/chat/__tests__/MessageList.test.tsx` with 11 tests
- Covers `bloomToHighlightClass` (6 levels + null/unknown), `renderHighlightedText` (matching, non-highlighted, empty phrases, case-insensitive, regex metachar escaping), `useBloomHighlight` (highlight on/off, 5s fade-out with fake timers, conversation switch clear, timer reset on new phrases)
- Tests failed at commit time (expected TDD red)

### Task 2 (TDD Green): MessageList implementation
- Added `bloomToHighlightClass`: maps Remember/Understand/Apply/Analyze/Evaluate/Create to bg tint classes (/10 cool, /15 warm)
- Added `renderHighlightedText`: capturing-group regex split, regex metacharacter escaping, `motion-reduce:transition-none` for accessibility, `rounded-sm transition-colors duration-300` per UI-SPEC
- Added `useBloomHighlight` hook: `bloomKeyPhrases` Zustand selector, local `isHighlighted` state, `setTimeout(5000)` fade-out with cleanup, immediate clear when phrases become empty
- Modified MessageList user message rendering: `shouldHighlight = isHighlighted && message.id === latestBloomMessageId` (D-07 guard)
- All 3 named exports added so tests can import them
- All 11 unit tests pass (TDD green)

### Task 3: promptfoo Bloom evaluator updates
- `prompt.txt`: added `key_phrases` to JSON output format with "Extract 1-4 short phrases (2-6 words each) verbatim" instruction
- `cases.yaml`: added key_phrases validation assertion to all 6 golden cases
- Each case has 2 assertions: original Bloom classification check + new key_phrases array validation
- Validates: Array.isArray, length 1-4, all elements non-empty strings

### Task 4: Visual verification (PENDING — checkpoint)
- Awaiting human visual verification in browser

## Task Commits

Each task was committed atomically:

1. **Task 1: Create MessageList highlight unit tests (TDD red)** - `b07d23a` (test)
2. **Task 2: Implement keyword highlight rendering in MessageList** - `efb3beb` (feat)
3. **Task 3: Add key_phrases assertions to promptfoo Bloom evaluator golden cases** - `1def01d` (feat)

## Files Created/Modified

- `src/components/chat/__tests__/MessageList.test.tsx` - 11 unit tests for bloomToHighlightClass, renderHighlightedText, useBloomHighlight
- `src/components/chat/MessageList.tsx` - bloomToHighlightClass, renderHighlightedText, useBloomHighlight exports + MessageList integration
- `prompt-eval/bloom-evaluator/cases.yaml` - key_phrases validation assertion on all 6 golden cases
- `prompt-eval/bloom-evaluator/prompt.txt` - key_phrases field added to JSON output format

## Decisions Made

- Capturing-group regex split used for highlighting — odd-indexed split elements are matched phrases, avoids lastIndex drift
- isHighlighted is local component state, not Zustand — transient animation state belongs in component
- motion-reduce:transition-none applied for prefers-reduced-motion accessibility (UI-SPEC requirement)
- D-07 guard implemented: `message.id === latestBloomMessageId` ensures only latest user message gets highlights

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - all rendering logic is fully wired. The feature is complete pending visual verification in Task 4.

## Self-Check

### Created files exist:
- `src/components/chat/__tests__/MessageList.test.tsx`: EXISTS
- `src/components/chat/MessageList.tsx` (modified): EXISTS

### Commits exist:
- `b07d23a`: EXISTS (test - TDD red)
- `efb3beb`: EXISTS (feat - TDD green)
- `1def01d`: EXISTS (feat - promptfoo cases)

## Self-Check: PASSED

---
phase: 999.2-evaluate-pretext-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "A written evaluation report exists with a clear No-Go verdict and reasoning"
    - "The report covers what Pretext is, comparison to current stack, and conditions for future reconsideration"
    - "The report follows the structure specified in D-11"
  artifacts:
    - path: ".planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md"
      provides: "Pretext integration evaluation report with No-Go verdict"
      contains: "No-Go"
  key_links: []
---

<objective>
Write a concise No-Go evaluation report for Pretext (chenglou) integration into NeuroGraph.

Purpose: Per D-04, research has conclusively shown Pretext is a text measurement library with zero overlap to NeuroGraph's chat rendering, AI streaming, or markdown needs. This plan produces the dismissal report and closes the evaluation phase.

Output: `999.2-EVALUATION.md` — the final deliverable for this evaluation phase.
</objective>

<execution_context>
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simone/Desktop/Projects/NeuroGraph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-evaluate-pretext-integration/999.2-CONTEXT.md
@.planning/phases/999.2-evaluate-pretext-integration/999.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Pretext evaluation report with No-Go verdict</name>
  <files>.planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md</files>
  <read_first>
    - .planning/phases/999.2-evaluate-pretext-integration/999.2-RESEARCH.md
    - .planning/phases/999.2-evaluate-pretext-integration/999.2-CONTEXT.md
  </read_first>
  <action>
Create `.planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md` as the final evaluation report. This is a quick dismissal per D-04 since Pretext is clearly irrelevant to NeuroGraph's needs.

Per D-11, structure the report with these sections:

1. **Header** — Title: "Phase 999.2: Pretext Integration Evaluation", date, verdict summary line: "Verdict: NO-GO — Pretext is a text measurement library with no overlap to NeuroGraph's needs"

2. **What Pretext Is** — Concise description: pure JS/TS text measurement and layout library by chenglou (creator of ReasonML, React Motion). Version 0.0.4 (published 2026-04-01). Zero runtime deps. Measures text height/line layout without DOM reflow. Target use cases: virtualized lists, canvas text, masonry layouts. NOT a markdown renderer, NOT an AI SDK, NOT a chat framework.

3. **Current NeuroGraph Stack** (per D-08) — Brief summary of what already handles text/chat:
   - `react-markdown` ^10.1.0 — markdown-to-React rendering in chat messages
   - `ai` ^6.0.82 (Vercel AI SDK) — streaming protocol, useChat hook
   - `@tiptap/react` ^3.20.4 — WYSIWYG editor for neuron content (separate from chat)
   - Chat rendering uses natural DOM flow in a scrollable div, no virtualization, no canvas, no custom measurement

4. **Comparison** (per D-08) — Key finding: Pretext and the current stack solve entirely different problems. react-markdown converts markdown AST to React elements. Pretext measures text dimensions without DOM. These are orthogonal. No component of NeuroGraph performs custom text measurement that Pretext could improve.

5. **Compatibility Notes for Other Call Sites** (per D-06) — Brief table: bloom evaluator (server-side LLM, no text layout — no relevance), architect pipeline (not rendering — no relevance), TipTap editors (ProseMirror handles own layout — no relevance).

6. **Go/No-Go Verdict** — **NO-GO** with three clear reasons:
   - Wrong domain: text measurement vs. markdown rendering — different problems
   - No problem to solve: NeuroGraph has no virtualized lists, canvas rendering, or expensive layout measurements
   - Pre-alpha risk: v0.0.4 with unstable API, no justification for adoption without a matching use case

7. **Conditions for Future Reconsideration** — Revisit only if: (a) NeuroGraph implements virtualized message list with measured performance problems, (b) NeuroGraph adds canvas/WebGL rendering for chat, (c) Pretext reaches 1.0.0 with stable APIs AND one of the above conditions applies.

8. **Migration Sketch** — "Not applicable (No-Go verdict)" per D-03.

Keep the report concise — this is a dismissal note per D-04, not a deep architectural analysis. Target 80-120 lines of markdown. All findings are drawn from 999.2-RESEARCH.md which was verified against live sources (npm registry, GitHub repo, NeuroGraph source code).
  </action>
  <verify>
    <automated>test -f .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md && grep -q "NO-GO" .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md && grep -q "text measurement" .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md && grep -q "react-markdown" .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md && grep -q "Future Reconsideration" .planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/999.2-evaluate-pretext-integration/999.2-EVALUATION.md` exists
    - File contains the string "NO-GO" (the verdict)
    - File contains the string "text measurement" (correctly identifies what Pretext is)
    - File contains "react-markdown" (comparison to current stack per D-08)
    - File contains "Future Reconsideration" or "Conditions" section (per D-03 inverse — explains when to revisit)
    - File contains "Migration Sketch" with "Not applicable" (per D-03)
    - File contains mention of bloom evaluator and architect pipeline (per D-06)
    - File is between 60 and 200 lines (concise dismissal per D-04, not a deep analysis)
  </acceptance_criteria>
  <done>A concise evaluation report exists at 999.2-EVALUATION.md with a clear No-Go verdict, current stack comparison, compatibility notes for all call sites, and conditions for future reconsideration. All decisions D-01 through D-11 are addressed.</done>
</task>

</tasks>

<verification>
- `999.2-EVALUATION.md` exists in the phase directory
- Report contains No-Go verdict with reasoning
- Report structure matches D-11 (what it is, capabilities, comparison, verdict, migration sketch)
- Report references D-04 early dismissal rationale
- All locked decisions (D-01 through D-11) are addressed in the report content
</verification>

<success_criteria>
A concise, well-reasoned evaluation report declares Pretext integration as No-Go, correctly identifying it as a text measurement library with zero overlap to NeuroGraph's markdown rendering, AI streaming, or chat UI needs. The report is the sole deliverable — no code changes were made.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-evaluate-pretext-integration/999.2-01-SUMMARY.md`
</output>

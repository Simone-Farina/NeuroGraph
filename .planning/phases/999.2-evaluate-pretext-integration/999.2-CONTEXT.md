# Phase 999.2: Evaluate Pretext (chenglou) Integration - Context

**Gathered:** 2026-04-03
**Status:** Ready for planning

<domain>
## Phase Boundary

Evaluate https://github.com/chenglou/pretext for potential integration into NeuroGraph. Research what Pretext actually is, assess general fit across the stack, and produce a Go/No-Go recommendation. This is a research phase — no code changes to NeuroGraph.

</domain>

<decisions>
## Implementation Decisions

### Evaluation Depth
- **D-01:** Feasibility report only — no prototype or code changes
- **D-02:** Report must include a Go/No-Go recommendation with clear reasoning
- **D-03:** If verdict is Go, include a migration sketch (which call sites to migrate, order, estimated effort)
- **D-04:** If clearly irrelevant after initial research, produce a quick dismissal note and close early

### Integration Target
- **D-05:** Primary evaluation focus on chat rendering/streaming path (/api/chat + ChatPanel UI)
- **D-06:** Include brief compatibility notes for other call sites (bloom evaluator, architect pipeline) even if not deeply evaluated

### Evaluation Criteria
- **D-07:** Open investigation — determine what Pretext actually is first, then assess general fit across any part of NeuroGraph (chat rendering, text formatting, AI layer, or otherwise)
- **D-08:** Compare against current stack (react-markdown, TipTap, Vercel AI SDK) — identify what Pretext offers that existing tools don't already handle
- **D-09:** Assess compatibility risk with existing infrastructure (Langfuse tracing, OpenRouter routing, Zustand state, streaming protocol)

### Deliverable Format
- **D-10:** Evaluation report as markdown document in `.planning/phases/999.2-evaluate-pretext-integration/`
- **D-11:** Report structure: what Pretext is, capabilities, comparison to current stack, Go/No-Go verdict, migration sketch if Go

### Claude's Discretion
- Report structure and section ordering
- Depth of source code analysis vs. documentation review
- Which specific aspects of Pretext to emphasize based on findings

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Pretext source
- `https://github.com/chenglou/pretext` — The library being evaluated (external)

### Current AI/rendering stack (for comparison)
- `src/app/api/chat/route.ts` — Current chat streaming implementation (Vercel AI SDK streamText)
- `src/components/chat/MessageList.tsx` — Current chat message rendering
- `src/components/chat/ChatPanel.tsx` — Chat panel container and wiring

No external specs — requirements fully captured in decisions above.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `react-markdown` 10.1.x — Current markdown rendering in chat messages
- TipTap v3 — WYSIWYG editor for neuron content (separate from chat rendering)
- Vercel AI SDK v6 (`ai` package) — Chat streaming via `streamText`, structured output via `generateObject`

### Established Patterns
- Chat streaming: `useChat` hook -> `/api/chat` -> `streamText` -> incremental UI updates
- Model routing: `getModelForRole()` strategy pattern for task-based model selection
- Observability: Langfuse OTel tracing wraps all AI call sites

### Integration Points
- Chat message rendering pipeline: AI response stream -> message state -> MessageList -> react-markdown
- AI SDK provider chain: OpenRouter -> model providers (Anthropic, OpenAI, Google)
- State management: Zustand stores (graphStore, chatStore) manage UI state

</code_context>

<specifics>
## Specific Ideas

- This was an "investigation note" backlog item — open-ended curiosity about the library, not a specific integration need
- If Pretext turns out to be a text rendering library (not AI-related), evaluation pivots to whether it improves chat message rendering vs. current react-markdown setup

</specifics>

<deferred>
## Deferred Ideas

- **Phase 27 (NGEN-01):** Restore in-chat neurogenesis suggestion instead of static GENERATE NEURON button — already scoped and tracked in Phase 27

None — discussion stayed within phase scope

</deferred>

---

*Phase: 999.2-evaluate-pretext-integration*
*Context gathered: 2026-04-03*

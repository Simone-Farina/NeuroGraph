---
phase: quick
plan: 260326-kz1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/ai/config.ts
  - src/lib/ai/providers.ts
  - src/app/api/chat/route.ts
  - src/app/api/bloom-evaluate/route.ts
  - src/app/api/architect/route.ts
  - src/app/api/neurons/extract/route.ts
  - src/app/api/neurons/ai-action/route.ts
  - src/app/api/neurons/curriculum/route.ts
  - src/app/api/neurons/[id]/synthesize/route.ts
  - src/lib/ai/synthesizer.ts
  - src/lib/ai/inferPrerequisites.ts
  - src/lib/crystallize/seed.ts
  - .env.example
  - src/app/api/chat/route.test.ts
  - src/app/api/architect/__tests__/route.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "OpenRouter models can be selected via env var (e.g. openrouter:anthropic/claude-3.5-sonnet)"
    - "All 4 agent roles are named conversationalist, bloomEvaluator, synthesizer, inquisitor"
    - "Changing AI_MODEL_* env vars switches provider at runtime without code changes"
    - "npx tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/lib/ai/config.ts"
      provides: "Agent role defaults and AgentRole type"
      exports: ["AI_AGENTS", "AgentRole"]
    - path: "src/lib/ai/providers.ts"
      provides: "getModelForRole with OpenRouter support"
      exports: ["getModelForRole", "getEmbeddingModel"]
  key_links:
    - from: "src/lib/ai/providers.ts"
      to: "src/lib/ai/config.ts"
      via: "imports AI_AGENTS, AgentRole"
      pattern: "import.*config"
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/ai/providers.ts"
      via: "getModelForRole('conversationalist')"
      pattern: "getModelForRole.*conversationalist"
---

<objective>
Unified multi-agent AI configuration with OpenRouter, Google, and OpenAI provider switching.

Purpose: Enable the founder to switch any of the 4 AI agents between OpenAI, Google, and OpenRouter providers via environment variables alone, using a single config file as the source of truth.
Output: `src/lib/ai/config.ts` (config), refactored `providers.ts` (with OpenRouter support), all consumers updated to new role names.
</objective>

<execution_context>
@/home/simone/.claude/get-shit-done/workflows/execute-plan.md
@/home/simone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/ai/providers.ts
@src/lib/ai/config.ts (to be created)
@.env.example

<interfaces>
<!-- Current providers.ts exports that will be refactored -->
From src/lib/ai/providers.ts:
```typescript
export type ModelRole = 'chat' | 'synthesis_fast' | 'neurogenesis_heavy' | 'evaluator';
export function getModelForRole(role: ModelRole): LanguageModelV1;
export function getEmbeddingModel(): EmbeddingModelV1;
```

<!-- Current consumer usage patterns (all must be updated) -->
Chat:              getModelForRole('chat')
Bloom evaluate:    getModelForRole('evaluator')
Architect:         getModelForRole('neurogenesis_heavy')
Synthesize:        getModelForRole('synthesis_fast')
Extract:           getModelForRole('synthesis_fast')
AI-action:         getModelForRole('synthesis_fast')
Curriculum:        getModelForRole('neurogenesis_heavy')
Synthesizer lib:   getModelForRole('evaluator')
InferPrereqs:      getModelForRole('neurogenesis_heavy')
Crystallize seed:  getModelForRole('synthesis_fast')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install OpenRouter SDK, create config.ts, refactor providers.ts</name>
  <files>package.json, src/lib/ai/config.ts, src/lib/ai/providers.ts, .env.example</files>
  <action>
1. Install the OpenRouter provider:
   ```
   npm install @openrouter/ai-sdk-provider
   ```

2. Create `src/lib/ai/config.ts` — the single source of truth for agent configuration:
   ```typescript
   // Agent role → env var name → default model string
   export const AI_AGENTS = {
     conversationalist: { env: 'AI_MODEL_CHAT',              default: 'openai:gpt-4o' },
     bloomEvaluator:    { env: 'AI_MODEL_EVALUATOR',         default: 'google:gemini-2.5-flash' },
     synthesizer:       { env: 'AI_MODEL_SYNTHESIZER',       default: 'openai:gpt-4o-mini' },
     inquisitor:        { env: 'AI_MODEL_INQUISITOR',        default: 'openrouter:anthropic/claude-3.5-sonnet' },
   } as const;

   export type AgentRole = keyof typeof AI_AGENTS;
   ```
   Note: Keep `AI_MODEL_CHAT` as the env var name for `conversationalist` to maintain backward compatibility with existing `.env.local` files. The synthesizer gets a new env var `AI_MODEL_SYNTHESIZER` (replaces `AI_MODEL_SYNTHESIS_FAST`). The inquisitor is a new role replacing `neurogenesis_heavy` with env var `AI_MODEL_INQUISITOR`.

3. Refactor `src/lib/ai/providers.ts`:
   - Remove the old `ModelRole`, `ROLE_ENV`, and `ROLE_DEFAULT` constants.
   - Import `AI_AGENTS, AgentRole` from `./config`.
   - Import `createOpenRouter` from `@openrouter/ai-sdk-provider`.
   - In `resolveFromString`, add an `openrouter` case:
     ```typescript
     case 'openrouter': {
       const openrouter = createOpenRouter({ apiKey: process.env.OPENROUTER_API_KEY });
       return openrouter(modelName);
     }
     ```
   - Update `getModelForRole` signature to accept `AgentRole` (re-export the type).
   - Read model string from `process.env[AI_AGENTS[role].env] || AI_AGENTS[role].default`.
   - Add a `console.log` at model resolution time: `[ai] ${role} → ${raw}` so the founder can see which model is active at startup.
   - Keep the `AI_PROVIDER=mock` guard and `getEmbeddingModel` unchanged.
   - Re-export `AgentRole` from providers.ts for backward compat: `export type { AgentRole } from './config';`

4. Update `.env.example`:
   - Rename `AI_MODEL_SYNTHESIS_FAST` → `AI_MODEL_SYNTHESIZER`
   - Rename `AI_MODEL_NEUROGENESIS_HEAVY` → `AI_MODEL_INQUISITOR`
   - Keep `AI_MODEL_CHAT` and `AI_MODEL_EVALUATOR` as-is
   - Add `OPENROUTER_API_KEY=sk-or-your-openrouter-key-here` in the API Keys section
   - Add `GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key-here` (rename from GOOGLE_API_KEY to match @ai-sdk/google convention)
   - Update the "Supported providers" comment to: `openai | anthropic | google | openrouter`
   - Update role descriptions to match new names (conversationalist, bloomEvaluator, synthesizer, inquisitor)
  </action>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && node -e "require('@openrouter/ai-sdk-provider')" && echo "OK"</automated>
  </verify>
  <done>config.ts exports AI_AGENTS and AgentRole. providers.ts resolves openrouter prefix. .env.example documents all 4 roles and 4 provider API keys.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all consumers to new AgentRole names and verify types</name>
  <files>
    src/app/api/chat/route.ts,
    src/app/api/bloom-evaluate/route.ts,
    src/app/api/architect/route.ts,
    src/app/api/neurons/extract/route.ts,
    src/app/api/neurons/ai-action/route.ts,
    src/app/api/neurons/curriculum/route.ts,
    src/app/api/neurons/[id]/synthesize/route.ts,
    src/lib/ai/synthesizer.ts,
    src/lib/ai/inferPrerequisites.ts,
    src/lib/crystallize/seed.ts,
    src/app/api/chat/route.test.ts,
    src/app/api/architect/__tests__/route.test.ts
  </files>
  <action>
Apply these mechanical renames across all consumer files. Each is a single-line change in the `getModelForRole(...)` call:

| File | Old Role | New Role |
|------|----------|----------|
| `src/app/api/chat/route.ts` | `'chat'` | `'conversationalist'` |
| `src/app/api/bloom-evaluate/route.ts` | `'evaluator'` | `'bloomEvaluator'` |
| `src/app/api/architect/route.ts` | `'neurogenesis_heavy'` | `'inquisitor'` |
| `src/app/api/neurons/extract/route.ts` | `'synthesis_fast'` | `'synthesizer'` |
| `src/app/api/neurons/ai-action/route.ts` | `'synthesis_fast'` | `'synthesizer'` |
| `src/app/api/neurons/curriculum/route.ts` | `'neurogenesis_heavy'` | `'inquisitor'` |
| `src/app/api/neurons/[id]/synthesize/route.ts` | `'synthesis_fast'` | `'synthesizer'` |
| `src/lib/ai/synthesizer.ts` | `'evaluator'` | `'bloomEvaluator'` |
| `src/lib/ai/inferPrerequisites.ts` | `'neurogenesis_heavy'` | `'inquisitor'` |
| `src/lib/crystallize/seed.ts` | `'synthesis_fast'` | `'synthesizer'` |

Also update the test files:
- `src/app/api/chat/route.test.ts`: Update any mock references to `ModelRole` → `AgentRole`
- `src/app/api/architect/__tests__/route.test.ts`: Same

If any file imports `ModelRole` type directly, change to `AgentRole`.

After all renames, run `npx tsc --noEmit` to confirm zero type errors across the entire codebase.
  </action>
  <verify>
    <automated>cd /home/simone/projects/NeuroGraph && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 10 consumer files use new AgentRole names. `npx tsc --noEmit` exits 0. No references to old role names (chat, synthesis_fast, neurogenesis_heavy, evaluator) remain in getModelForRole calls.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `grep -r "getModelForRole.*'chat'" src/` — returns zero matches (old role name gone)
3. `grep -r "getModelForRole.*'synthesis_fast'" src/` — returns zero matches
4. `grep -r "getModelForRole.*'neurogenesis_heavy'" src/` — returns zero matches
5. `grep -r "getModelForRole.*'evaluator'" src/` — returns zero matches (now 'bloomEvaluator')
6. `grep -r "getModelForRole.*'conversationalist'" src/` — returns matches in chat route
7. `grep -r "getModelForRole.*'inquisitor'" src/` — returns matches in architect + curriculum + inferPrereqs
</verification>

<success_criteria>
- OpenRouter provider resolves correctly in providers.ts switch statement
- config.ts is the single source of truth for agent role names, env vars, and defaults
- All 10 consumer files compile with new AgentRole type
- .env.example documents all 4 provider API keys and all 4 agent role env vars
- `npx tsc --noEmit` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260326-kz1-unified-multi-agent-ai-config-with-openr/260326-kz1-SUMMARY.md`
</output>

# Shared Prompt Eval Assets

This directory holds assets shared by multiple prompt-eval suites:

- local provider scripts
- JSON schemas
- helper docs

Phase 10 uses `neurograph-bouncer-provider.mjs` plus the shared Bouncer response schema.
